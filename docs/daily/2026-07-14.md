## backend-developer (lot service) — PH-DL-03 + PH-DL-04

`app/services/download_service.py` (new) et `app/workers/download_worker.py` (new) livrés sur `develop`, plus
l'append `DOWNLOAD_*` dans `app/config.py` (`DOWNLOAD_DIR`, `DOWNLOAD_CONCURRENCY`, `DOWNLOAD_CHUNK_BYTES`,
`DOWNLOAD_MAX_RETRIES`, `DOWNLOAD_MIN_FREE_DISK_MB`, `DOWNLOAD_POLL_INTERVAL`, `DOWNLOAD_CONNECT_TIMEOUT`,
`DOWNLOAD_READ_TIMEOUT`) — périmètre strictement respecté (3 fichiers, aucun autre touché). Toutes les signatures
figées §5/§6 de `docs/20-impl-media-download.md` sont implémentées à l'identique : `enqueue_selection` (dédup par
job non-terminal, film ⇒ pas de batch, série ⇒ 1 `download_batch` + N jobs via une requête locale sur `Media`,
`media_service.py` non touché), `compute_dest_path`/`resolve_confined` (confinement F-007 : sanitize défensif
par segment — NFC, contrôles Unicode Cc/Cf/Cs, collapse `..`, noms réservés Windows — puis preuve réelle par
`realpath` sous `DOWNLOAD_DIR`), `download_to_disk` (reprise `Range`/`.part`, promotion atomique `os.replace`,
jamais l'URL Xtream dans un message d'erreur), `list_jobs`/`cancel_job`/`retry_job`/`clear_finished`,
`to_download_response`+`compute_percent`/`compute_speed_bps`. Le worker (`reap_orphans`/`run_drain_loop`/
`_run_job`) implémente le modèle de concurrence 100% DB (claim/progress/cancel/terminal via `UPDATE`
conditionnels cross-process) avec `run_with_retry` sur chaque écriture et session fraîche par tentative — prêt
à être appelé mais pas encore démarré (wiring = PH-DL-06, hors périmètre). Le lot routes (PH-DL-05) a intégré
en parallèle contre ces mêmes signatures sans ajustement nécessaire.

Vérifs : imports OK, `ruff check` vert sur les 3 fichiers, `pytest -v` = 735 passed (baseline inchangée, 0
régression). Point d'attention pour la suite : la gate de couverture globale (`--cov-fail-under=70`) est
désormais rouge (68.09%) car les 2 nouveaux fichiers (~490 lignes de logique) n'ont encore aucun test —
attendu, hors périmètre de ce ticket (tests = PH-DL-07/qa-engineer), se résorbera une fois ces tests écrits.

`docs/31-board.md` mis à jour (PH-DL-03/PH-DL-04 → Done). Next : PH-DL-06 (wiring `main.py`, seul éditeur),
PH-DL-07 (tests), security-reviewer (R2 sur écriture FS confinée + creds Xtream).

## backend-developer (lot wiring) — PH-DL-06

Dernier maillon câblé, strictement dans `app/main.py` (seul fichier touché). Deux routers montés : `admin_downloads.router`
derrière le même garde `Depends(verify_admin_basic_auth)` que `admin.router` (juste après son mount) ; `downloads.router`
monté nu en Pattern C (déjà self-préfixé `/api/admin/downloads` + self-guardé `verify_master_key`, même convention que
`api_keys.router`/`ai.router`). Worker master-only : `reap_orphans`+`run_drain_loop` regroupés dans une coroutine
`_run_download_worker` (garde locale `if not settings.DOWNLOAD_DIR: return`, no-op total feature désactivée) passée à
`create_background_task(..., name="download_worker")` dans le bloc `if is_master:`, juste après `scheduler.start()` et
**avant** la création de la tâche `initial_sync` — ordre volontaire : `tests/test_startup_wiring.py` capture le *dernier*
appel à `create_background_task` dans un dict simple et assert `name == "initial_sync"` ; l'inverser aurait cassé ce test
sans toucher à son fichier (hors périmètre). Le tout est non-bloquant (aucun `await` direct de `reap_orphans`/
`run_drain_loop` dans le corps du lifespan), cohérent avec le pattern déjà en place pour `initial_sync_then_enrich`, et
n'accède jamais à la vraie DB pendant les tests de lifespan (le `create_background_task` y est monkeypatché, jamais awaité).
Vérifié empiriquement (ASGI in-process, clés d'env positionnées) : `/admin/downloads` 401→200 (Basic Auth), `/api/admin/downloads`
401→200 (`X-API-Key` maître), `/api/health` 200 inchangé. `import app.main` OK, OpenAPI **60→70 paths**. `pytest -v` =
**735 passed** (0 régression, `test_startup_wiring.py` 7/7 verts) ; gate de couverture reste rouge (69.19%, pré-existant
PH-DL-03/04/05, hors périmètre — se résorbera avec PH-DL-07). `ruff check app/main.py` vert. `docs/31-board.md` mis à jour
(PH-DL-06 → Done). Next : PH-DL-07 (qa-engineer, tests), code-reviewer (R1), security-reviewer (R2 sur PH-DL-03/04/05).
