## backend-developer — X3 (UI admin « Télécharger » Xtream — granulaire + taille)

Onglet admin Xtream `/admin/downloads` aligné sur le socle service X1a/X1b/X2
(`download_service.estimate_media_size`/`list_series_seasons_with_sources`/
`list_series_episodes_with_sources`/`enqueue_selection(scope='seasons'|'episodes')`) et mis en
MIROIR STRICT de l'onglet « Télécharger Plex » (`admin_plex_downloads.py`) livré la veille (C6).

**Modifiés** : `app/api/admin_downloads.py` (routeur — `_fmt_size`/`_enrich_source_dict`
copiés de `admin_plex_downloads` ; route `/versions` réécrite : movie = table Serveur/Taille/État
avec `estimate_media_size` par version, show = sources « série entière » SANS taille + `seasons`
via `list_series_seasons_with_sources` ; POST enqueue réécrit sur le même schéma que Plex —
`source="server_id|rating_key"` pour `movie`/`series_all`, `season_pick`/`episode_pick` répétés
pour les 2 nouveaux scopes `seasons`/`episodes` ; `_parse_source`/`_parse_season_picks`/
`_parse_episode_picks` copiés à l'identique). **Créés** : nouvelle route
`GET /admin/downloads/{type}/{unification_id:path}/episodes?season=N` (fragment sélecteur
par-épisode) ; `app/templates/admin/_downloads_episodes.html` (nouveau, miroir de
`_plex_downloads_episodes.html`). **Réécrit** : `app/templates/admin/_downloads_versions.html`
(movie = colonne Taille avec suffixe `~` si estimée ; show = 3 blocs « Série entière »/« Par
saison » (`<select name="season_pick">` value `"{season}|{server_id}|{series_rating_key}"`,
label avec taille + nb épisodes) /« Par épisode » (boutons HTMX → `#dl-episodes-{season}`)).

Le scope `series_seasons` (checkboxes, une seule source) **disparaît de l'UI** — remplacé par
`seasons` (un `<select>` par saison, une source distincte possible par saison/compte) ; le service
le garde intact pour ses propres tests (`enqueue_selection` continue de le supporter, non appelé
par ce routeur). Aucune migration — 100 % lecture/service déjà livrés X1a/X1b/X2.

**Tests** : `tests/test_admin_downloads.py` créé (23 tests — versions movie avec taille exacte
`4.2 Go`/estimée `572.2 Mo` + `~`, versions show « série entière »+picker saison avec taille
sommée, fragment épisodes avec picker+taille, enqueue movie/series_all/seasons/episodes dont
multi-compte 1 batch, gardes 401 sur les 2 nouvelles routes). `tests/test_download_security.py`
mis à jour : `test_enqueue_401_without_credentials` utilisait encore l'ancien format de form
(`server_id`/`rating_key`) — passé au nouveau (`source="server_id|rating_key"`) pour rester
représentatif du contrat réel (le 401 lui-même ne dépendait pas du format, mais un futur test de
succès sur ce endpoint aurait planté en 422).

Vérifs : `ruff check app/api/admin_downloads.py tests/test_admin_downloads.py
tests/test_download_security.py` vert. `python -c "from app.main import app"` OK, routes
`GET/POST /admin/downloads` + `GET /admin/downloads/{type}/{id}/versions|episodes` confirmées
enregistrées. `pytest tests/test_admin_downloads.py tests/test_download_security.py -v` =
**50 passed**. Suite complète `pytest -q` = **1121 passed**, couverture **76.34 %** (gate
`--cov-fail-under=70` verte), 0 régression. Next : code-reviewer.
