# Board — Feature « Télécharger » (téléchargement physique de médias)

> Spec : `docs/20-impl-media-download.md` · PRD : `docs/10-prd-media-download.md` · ADR : `docs/architecture/adr/0002-media-download-writes-to-disk.md`
> But du sprint : livrer les P0 F-001→F-008 (onglet admin + file persistante + worker master-only + confinement) sans régression.

## Definition of Done (tout ticket)
- Code sur `develop` · `pytest -v` **vert** · boot `uvicorn app.main:app` OK · `GET /api/health` **200**
- Migrations **idempotentes** (re-run sans casse) · **OpenAPI à jour** (schémas Pydantic v2, pas de dict nu)
- `run_with_retry` sur tout writer · aucun secret Xtream loggé · code-reviewer approuvé (+ security-reviewer si FS/creds)
- QA a exercé les critères d'acceptation du PRD §5

## Légende
- **Safe/Risky** : Risky = migration de schéma (needs-approval) **ou** surface sécurité (écriture FS + creds dans URLs).
- **Depends on** : arête dure (hard) = bloque le démarrage ; (soft) = contrat figé dans la spec, dev en parallèle, intégration au merge.

## Tickets

| PH-NNN | F-NNN | Titre | Owner | Status | Depends on | Safe/Risky | Estimate | Périmètre fichiers (DISJOINT) | Acceptance (PRD) |
|---|---|---|---|---|---|---|---|---|---|
| **PH-DL-01** | F-008 | Racine : migration 018 + entités ORM + schémas Pydantic | `db-migration-specialist` | **Done** | — | **Risky** (schéma, needs-approval) | M | `app/db/migrations.py` (append 018), `app/models/database.py` (append `DownloadJob`,`DownloadBatch`), `app/models/schemas.py` (append `Download*`) | US-008.1 : 018 idempotente en fin de chaîne, boot OK, `/api/health` 200 — **validé** : `_migration_018_create_download_tables` rejouée 2× (fresh DB via `create_all` + DB "upgradée" simulée) sans erreur ; `init_db()` (create_all+PRAGMA+migrations) rejoué 2× sur la DB réelle OK ; `app.main:app`/`openapi()` s'importent (60 routes, aucune régression) ; `pytest -v` = 735 passed (cov 73.03 %) ; `ruff check` vert sur les 3 fichiers. Note d'écart : le builder `to_download_response`/`compute_percent`/`compute_speed_bps` (§4/§6.4 de la spec) est **volontairement laissé à `download_service.py`** (PH-DL-03), pas ajouté ici dans `schemas.py` — la spec l'autorise dans l'un ou l'autre fichier, et `schemas.py` a un éditeur unique (cette ticket) par la règle de non-collision du board : un builder que PH-DL-03/04/05 doivent pouvoir étendre doit vivre dans un fichier qu'ils possèdent. |
| **PH-DL-03** | F-003/F-005/F-007 | `download_service` : enqueue, `compute_dest_path`+confinement, `download_to_disk`, list/cancel/retry + config `DOWNLOAD_*` | `backend-developer` (lot service) | **Done** | PH-DL-01 (hard) | **Risky** (FS + creds) | L | `app/services/download_service.py` (new), `app/config.py` (append) | US-003.1/3.3, US-005.1, US-007.1 : jobs persistés, `.part`→rename, 0 écriture hors `DOWNLOAD_DIR` — **implémenté** : toutes les signatures §5 de la spec respectées à l'identique (`enqueue_selection`/`list_jobs`/`get_job`/`cancel_job`/`retry_job`/`clear_finished`/`compute_dest_path`/`resolve_confined`/`download_to_disk`/`to_download_response`+`compute_percent`/`compute_speed_bps`). Confinement F-007 : `_sanitize_segment` (NFC, strip séparateurs + contrôles Unicode Cc/Cf/Cs, collapse `..`, cap 180, garde noms réservés Windows) + `resolve_confined` (realpath sous `DOWNLOAD_DIR`, seule preuve réelle). Tous les writers (`enqueue`/`cancel`/`retry`/`clear_finished`) passent par `run_with_retry`. `is_adult` câblé dans `compute_dest_path` mais **non appliqué** par `enqueue_selection` (décision figée §12, P2). URL Xtream jamais construite ici (le worker la dérive, non persistée). `ruff check` vert ; `pytest -v` = 735 passed (inchangé) mais **gate de couverture globale rouge** (68.09 % < 70 %, faute de tests sur les 2 nouveaux fichiers — attendu, PH-DL-07 n'a pas encore atterri). |
| **PH-DL-04** | F-003/F-004/F-005/F-006 | `download_worker` : drain master-only, reap boot, progression, auto-retry, cancel coopératif | `backend-developer` (lot service) | **Done** | PH-DL-03 (hard), PH-DL-01 (hard) | **Risky** (FS + creds) | L | `app/workers/download_worker.py` (new) | US-004.1/4.2, US-005.2, US-006.1 : progression persistée, pas de `running` fantôme au boot, `failed` sans URL — **implémenté** : `reap_orphans`/`run_drain_loop`/`_run_job` prêts à être appelés (démarrage effectif = PH-DL-06). Claim/progress/cancel/terminal 100 % via `UPDATE` conditionnels cross-process (spec §6.2), session fraîche par tentative + `run_with_retry` sur CHAQUE écriture (claim, progress throttlé ≥1s, terminal, reap). Backoff transitoire `min(2**attempts,30)s` avec `state` gardé `running` pendant le sleep (le job re-`queued` seulement après, sous garde `WHERE state='running'`) pour ne jamais laisser le drain loop repolling prématurément. `_safe_error` borne à 200 car. et ne reçoit jamais l'exception httpx brute (chaînes cassées via `from None` dans `download_to_disk` pour éviter toute fuite d'URL par traceback). Non wiré dans `main.py` (hors périmètre, PH-DL-06). `ruff check` vert ; `pytest -v` = 735 passed (inchangé), même remarque de couverture que PH-DL-03. |
| **PH-DL-05** | F-001/F-002/F-004/F-006/F-101 | Routes+templates : HTMX `/admin/downloads` + JSON `/api/admin/downloads` (P1 lecture) + nav | `backend-developer` (lot routes) | **Done** | PH-DL-01 (hard), PH-DL-03 (**soft** — signatures figées §5) | **Risky** (surface admin FS) | M | `app/api/admin_downloads.py` (new), `app/api/downloads.py` (new), `app/templates/admin/downloads.html`+`_downloads_*.html` (new), `app/templates/admin/base.html` (edit nav) | US-001.1/1.2, US-002.1/2.2, F-004 queue, F-101 : 200/401/404 attendus, liste unifiée = `/api/media/*/unified` — **validé** (ASGI in-process client, routers mounted standalone since PH-DL-06 wiring is a separate ticket): `GET /admin/downloads` 200, `/list?type=movie\|show` 200 filtré, `/{type}/{unification_id:path}/versions` 200 (movie via `get_unified_group`, show via the SAME call — PRD §3 "chaque version = une source de série (compte)", not per-episode `get_unified_episodes` as the spec's route table literally names — see commit note), unknown id 404 ; full enqueue→queue→cancel→retry→clear-finished cycle 200 against the real `download_service` (landed concurrently by the service lot mid-ticket) for both `scope=movie` and `scope=series_all` ; `DOWNLOAD_DIR` unset → 200 error fragment, 0 job ; JSON mirror `GET /api/admin/downloads` 401 without the master key, 200 + camelCase `DownloadJobListResponse` with it. **Bug found+fixed in this ticket**: `unification_id` values contain literal `://` (e.g. `imdb://tt0088247`) — FastAPI's default single-segment path converter 404s on that (verified empirically, both raw and percent-encoded); routed with Starlette's `{unification_id:path}` converter instead. `pytest -v` = 735 passed, 0 regression (own files import-guarded — `admin_downloads.py`/`downloads.py` are not yet mounted in `main.py`, PH-DL-06); `ruff check` clean on all new/edited files. Coverage floor (`--cov-fail-under=70`) currently red (68.09%) — **not from this ticket's files** (0 new tested statements either way, routers unmounted) but from `download_service.py`/`download_worker.py` landing 0%-covered ahead of PH-DL-07; resolves once QA's tests land per the board's sequencing. |
| **PH-DL-06** | F-003/F-008 | Wiring : mount des 2 routers + start `run_drain_loop` (master) | `backend-developer` (lot wiring) | **Done** | PH-DL-04 (hard), PH-DL-05 (hard) | Safe (2 lignes, mais touche lifespan) | S | `app/main.py` (edit — **seul** éditeur) | Worker démarre master-only ; routers montés ; boot OK — **validé** : `admin_downloads.router` monté sur `dependencies=[Depends(verify_admin_basic_auth)]` (même garde/style que `admin.router`, juste après) ; `downloads.router` monté nu (Pattern C, déjà self-guardé `verify_master_key` + self-préfixé `/api/admin/downloads`, comme `api_keys.router`) ; import ajouté à la ligne d'import `app.api` existante. Worker : `reap_orphans`+`run_drain_loop` regroupés dans **une seule** coroutine `_run_download_worker` (garde locale `if not settings.DOWNLOAD_DIR: return` — miroir du garde interne de `run_drain_loop`, no-op total si la feature est désactivée) passée à `create_background_task(..., name="download_worker")` **dans `if is_master:`, juste après `scheduler.start()` et avant** `create_background_task(initial_sync_then_enrich(), name="initial_sync")` — l'ordre est déterminant : `tests/test_startup_wiring.py::TestLifespanMasterElection` capture le **dernier** appel à `create_background_task` dans un dict simple (pas une liste) et assert `captured_bg.get("name") == "initial_sync"` ; placer le download-worker AVANT préserve ce test tel quel (7/7 passed, aucune édition de fichier de test). Le tout est non-bloquant (jamais de `await` direct de `reap_orphans`/`run_drain_loop` dans le corps du lifespan) — cohérent avec le pattern déjà en place pour `initial_sync_then_enrich`, et évite tout accès DB réel pendant `TestLifespanMasterElection` (le double `create_background_task` y est monkeypatché et n'awaite jamais son argument). Vérifié empiriquement (ASGI in-process, env `AI_API_KEY`/`ADMIN_USERNAME`/`ADMIN_PASSWORD` positionnées) : `GET /admin/downloads` 401 sans creds → 200 avec Basic Auth ; `GET /api/admin/downloads` 401 sans clé → 200 avec `X-API-Key` maître ; `/api/health` 200 inchangé. `python -c "import app.main"` OK, `app.openapi()` passe de **60 → 70 paths**. `pytest -v` = **735 passed** (inchangé, 0 régression — `tests/test_startup_wiring.py` 7/7) ; gate `--cov-fail-under=70` reste rouge (69.19%, **pré-existant depuis PH-DL-03/04/05** — `download_service.py`/`download_worker.py` non couverts avant l'atterrissage de PH-DL-07, hors périmètre de ce ticket qui ne touche que `main.py`). `ruff check app/main.py` vert. |
| **PH-DL-07** | F-001..F-008/F-101 | Plan + exécution tests pytest (respx, SQLite éphémère) | `qa-engineer` | **Done** | spec (plan) ; exécution après PH-DL-03/04/05 | Safe | M | `tests/test_download_*.py` (new), `tests/conftest.py` (fixture `download_dir`) | **Livré** : 4 suites (`confinement`/`security`/`service`/`transfer`, 1405 LOC, **121 tests**). Résultat : **856 passed** (735 baseline + 121), coverage **73.11 %** (gate 70 % rétabli), 0 régression. `download_service.py` couvert à 97 %. Confinement F-007 prouvé (canary au parent du dir), non-fuite creds, cycle enqueue→cancel→retry, reprise Range respx, reap_orphans, rejet auth 401. |
| **PH-DL-R1** | — | Review continue (contrat + conventions) | `code-reviewer` | continu | par ticket | Safe | — | (diff review) | Spec + conventions Python/FastAPI |
| **PH-DL-R2** | — | Review sécurité (FS confiné + creds Xtream) | `security-reviewer` | continu | PH-DL-03/04/05 | Safe | — | (diff review) | 0 fuite creds, confinement prouvé, pas de chemin client |
| **PH-DL-08** | review remediation | Correctifs consolidés post-review (code-reviewer + security-reviewer) : résilience drain loop, anti-SSRF redirect, préflight disque, throttle cancel-check, fix HOL blocking + 4 nits | `backend-developer` | **Done** | PH-DL-03/04/05/06/07 (hard) | Safe (additif, aucun changement de schéma/contrat public) | M | `app/workers/download_worker.py`, `app/services/download_service.py`, `app/config.py` (commentaire), `app/main.py` (retire le double `reap_orphans`), `app/api/admin_downloads.py`, `app/api/downloads.py`, `app/templates/admin/downloads.html`+`_downloads_versions.html`, `tests/test_download_*.py` | **Livré** — 6 correctifs de comportement + 4 nits, tous sous test de garde : **(1) BLOQUANT** `run_drain_loop` enveloppe désormais chaque tick dans `try/except Exception` (log `exc_info=True`, jamais d'URL, puis `continue` après le sleep de poll) — une `OperationalError` non-lock (checkpoint WAL, backup nocturne, "disk image malformed") ne tue plus la boucle définitivement ; `asyncio.CancelledError` (BaseException) continue de se propager au shutdown, testé explicitement. **(2) Sécu Moyen DL-01** `httpx.AsyncClient(follow_redirects=False)` dans `download_to_disk` — tout 3xx upstream devient un `DownloadPermanentError` (déjà géré par la branche générique `status_code not in (200,206)`, donc **aucune** régression de comportement pour les cas non-redirect) ; testé qu'une réponse 302/301/303/307/308 ne déclenche **jamais** de fetch de la cible `Location`. **(3) Sécu Moyen DL-02** `DOWNLOAD_MIN_FREE_DISK_MB` câblé via `download_service.check_free_disk_space()` (nouvelle fonction, `shutil.disk_usage` offloadé `asyncio.to_thread`), appelée dans `_run_job` juste avant `download_to_disk` (même bloc `try/except DownloadPermanentError`) → `InsufficientDiskSpaceError` (sous-classe de `DownloadPermanentError`, ne consomme aucun retry) ; `<=0` = désactivé. **(4) Majeur** `cancel_check` throttlé au même gate monotonic 1 s que `on_progress` (nouveau dict `last_cancel_check` dans `_run_job`) — passe de 1 SELECT/chunk à ~1 SELECT/s. **(5) Majeur (HOL blocking)** `_handle_transient` réécrit la ligne `state='queued'` **avant** le `asyncio.sleep(delay)` (au lieu d'après) et `_run_job` ne l'appelle plus **qu'après** avoir quitté `async with sem:` → à `DOWNLOAD_CONCURRENCY=1`, un job en échec transitoire n'affiche plus `running` pendant son back-off (il montre `queued`) et ne bloque plus la file (le sémaphore est libéré immédiatement, un 2ᵉ job prêt peut démarrer pendant que le 1ᵉʳ patiente) — prouvé par un test qui fait tourner un vrai 2ᵉ job à complétion pendant que le 1ᵉʳ est en back-off simulé. **CR-MIN-1** : retiré l'appel explicite `reap_orphans` dans `main.py::_run_download_worker` (doublon — `run_drain_loop` l'appelle déjà en premier) ; ordre des `create_background_task` (download-worker AVANT initial_sync) préservé, `tests/test_startup_wiring.py` inchangé et vert. **CR-MIN-2** : sur une réponse 206, si le `Content-Range` renvoyé ne démarre pas exactement à `resume_from` demandé (provider non conforme), le `.part` est tronqué et redémarré comme un `200` (au lieu d'être *appended*, ce qui aurait laissé un trou/chevauchement) ; absence totale de `Content-Range` = comportement inchangé (append, lenient). **CR-NIT-1/2** : suppression du filtre Jinja `ms`/`_fmt_ms` mort dans `admin_downloads.py` (jamais utilisé par les templates `_downloads_*.html`) et de la garde `try/except ImportError`/`_require_service`/`download_service is None`/`service_available` — morte depuis que `download_service`/`download_worker` ont atterri (PH-DL-03/04), nettoyée dans `admin_downloads.py`, `downloads.py` et les 2 templates concernés. **Tests** : nouveau fichier `tests/test_download_worker.py` (8 tests : résilience drain loop ×2, préflight disque ×2, throttle cancel-check, HOL blocking ×2, requeue-avant-sleep) + extension de `test_download_transfer.py` (+4 : no-redirect ×2, Content-Range mismatch ×2) + `test_download_service.py` (+4 : `check_free_disk_space`). Résultat : **876 passed** (856 + 20), coverage **74.08 %** (`download_worker.py` 44 %→82 %), `ruff check` vert sur tout le repo, boot + `/api/health` 200 vérifiés (ASGI in-process). |

## DAG (arêtes de dépendance)

```
PH-DL-01 (migration+ORM+schemas)  ── hard ──▶ PH-DL-03 (service+config) ── hard ──▶ PH-DL-04 (worker) ── hard ──┐
      │                                                                                                          ├──▶ PH-DL-06 (wiring main.py)
      └── hard ──▶ PH-DL-05 (routes+templates) ◀── soft (contrats §5 figés) ── PH-DL-03 ────────── hard ─────────┘

PH-DL-07 (tests) : plan dès la spec ; exécution après 03/04/05.
Reviews (R1 continu, R2 sur 03/04/05).
```

## Ce qui tourne EN PARALLÈLE vs EN SÉRIE

**EN SÉRIE (spine série)** :
1. `PH-DL-01` (racine — migration/ORM/schemas fige les entités et le contrat DB). **Doit finir en premier** : Risky/needs-approval.
2. puis `PH-DL-03` (service+config) → `PH-DL-04` (worker) : le worker consomme les primitives du service (dépendance dure, même track/owner).
3. puis `PH-DL-06` (wiring `main.py`) : intégration finale, **seul** éditeur de `main.py` (évite tout conflit sur le god-file).

**EN PARALLÈLE (dès PH-DL-01 mergé)** :
- `PH-DL-05` (routes+templates, 2ᵉ dev) démarre **en parallèle** de `PH-DL-03/04` : fichiers **disjoints** (`api/` + `templates/`
  vs `services/` + `workers/`) et signatures de `download_service` **figées §5 de la spec** → dev contre l'interface, intégration au merge.
- `PH-DL-07` (qa) : **plan de test** écrit dès la spec (parallèle total) ; **exécution** après que 03/04/05 atterrissent.
- `PH-DL-R1`/`R2` : review en continu au fil des merges.

**Contrainte de non-collision** : `main.py`, `schemas.py`, `models/database.py`, `migrations.py` ont chacun **un seul** éditeur
(lot wiring pour `main.py` ; lot racine pour les 3 autres) → aucun merge-conflict entre tracks parallèles.

## Lancement parallèle (handoff tech-manager)

```
SPRINT « Télécharger » — But : P0 F-001→F-008 + F-101 lecture, additif, 0 régression
Lancement immédiat :
- db-migration-specialist  ← PH-DL-01 (migration 018 + ORM + schemas)        [P0, Risky/needs-approval]  ← RACINE
Dès PH-DL-01 mergé (parallèle) :
- backend-developer (A, lot service)  ← PH-DL-03 → PH-DL-04                    [P0, Risky]
- backend-developer (B, lot routes)   ← PH-DL-05 (contrats service figés §5)   [P0, Risky]
Puis (série) :
- backend-developer (wiring)          ← PH-DL-06 (après PH-DL-04 + PH-DL-05)    [P0]
Continu :
- qa-engineer      ← PH-DL-07 (plan dès maintenant, exécution après le code)   [P0]
- code-reviewer    ← R1 (tous)     · security-reviewer ← R2 (PH-DL-03/04/05)
Stretch (si capacité) : F-102 préflight disque · F-103 métriques · F-104 clear-finished · F-201/202/203 (P2)
DoD : pytest -v vert · boot uvicorn app.main:app · /api/health 200 · migrations idempotentes · OpenAPI à jour
```

## Follow-ups post-review (P2/P3) — délibérément NON corrigés dans PH-DL-08

Issus de la passe de review consolidée (code-reviewer + security-reviewer) sur la feature « Télécharger ». Chacun a
une raison explicite de ne **pas** être corrigé maintenant — tracé ici pour ne pas être reperdu, pas pour être codé
dans l'immédiat.

| ID | Titre | Sévérité | Pourquoi non corrigé maintenant | Owner suggéré |
|---|---|---|---|---|
| **DL-03** | Pas de protection CSRF sur `POST /admin/downloads*` (enqueue/cancel/retry/clear-finished) | P2 (sécu) | Aligné sur la dette **existante CR-S07** (« CSRF sur `/admin` — Basic Auth + form POST, pas de token ») qui couvre **déjà tout** `/admin` (pas seulement `/downloads`) — corriger uniquement les routes download serait incohérent avec le reste de l'UI admin et laisserait une fausse impression de couverture. À traiter en un seul effort transverse sur `/admin` entier (session/token CSRF Jinja+HTMX), pas feature par feature. | `security-reviewer` puis `backend-developer` (effort dédié `/admin`) |
| **DL-04** | Hasards de nom de chemin Windows (`:` dans un titre, noms de fichier réservés + extension, ex. `CON.mkv`) | P2/P3 | Cible de déploiement = **Docker/Linux** (ADR `docs/architecture/adr/0002-media-download-writes-to-disk.md`) — non exploitable sur la cible réelle. `_sanitize_segment` gère déjà les noms réservés Windows nus (`CON`, `NUL`, `COM1`…) en défense-en-profondeur ; reste le cas `CON.mkv` (réservé + extension) sur un déploiement Windows hypothétique, hors périmètre. | `backend-developer`, si/quand une cible Windows native est envisagée |
| **DL-05** | TOCTOU symlink entre `resolve_confined` (check) et l'écriture réelle (`open(part, mode)`/`os.replace`) | P2/P3 | Même ADR 0002 : cible Docker/Linux avec `DOWNLOAD_DIR` sous contrôle opérateur (pas de multi-tenant écrivant dans l'arbre) — fenêtre TOCTOU non exploitable par un tiers non privilégié dans ce modèle de menace. `TestSymlinkEscape` (`tests/test_download_confinement.py`) couvre déjà le cas symlink statique (créé avant l'appel) ; le cas dynamique (race pendant l'écriture) resterait un effort dédié (`os.open(O_NOFOLLOW)`/refresh du realpath post-écriture) si le modèle de menace change. | `security-reviewer` (réévaluation si le modèle de menace change) |
| **F-103** | Métriques Prometheus dédiées téléchargement (`plexhub_download_*` : jobs actifs, débit, taux d'échec) | P1 non livré au MVP | Explicitement scoping-out du sprint MVP (voir « Stretch » ci-dessus) — le suivi opérationnel actuel passe par `GET /admin/downloads/queue` (polling HTMX) et `GET /api/admin/downloads` (JSON, QA/automation), suffisant pour le MVP. Pas de régression : les métriques HTTP génériques (instrumentator) couvrent déjà les routes `/admin/downloads*`/`/api/admin/downloads*`. | `observability-analyst` / `backend-developer` (sprint suivant) |

DoD de ces follow-ups (quand ils seront pris) : même gate que tout ticket de ce board (§ Definition of Done ci-dessus)
+ test de garde dédié à chaque finding.

## Follow-ups post-review (P2/P3) — feature « Télécharger Plex » (tickets C1→C7)

Feature miroir de « Télécharger » (Xtream) mais avec une source de catalogue distincte (serveurs Plex Media
Server partagés via plex.tv, tables `plex_server`/`plex_media_item`/`plex_sync_status`, migration 019).
PRD : `docs/10-prd-plex-download.md`. Réutilise intégralement la file `download_job` + le worker
`download_worker` de la feature Xtream (discriminant `server_id` préfixe `plex_`) — les follow-ups
ci-dessous concernent **le nouveau périmètre spécifique Plex** (découverte/catalogue/sources), pas le
worker/la file déjà couverts par les follow-ups DL-03/04/05 ci-dessus.

| ID | Titre | Sévérité | Pourquoi non corrigé maintenant | Owner suggéré |
|---|---|---|---|---|
| **DL-PLEX-01** | Pas de re-probe automatique de connexion au moment du téléchargement | P2 | `plex_server.is_reachable`/`base_uri` ne sont rafraîchis qu'au sync catalogue (manuel ou cron `PLEX_SYNC_INTERVAL_HOURS`) ; si le serveur change d'IP/port entre deux syncs, le job échoue proprement (`failed`, retries épuisés) plutôt que de re-probe à la volée — comportement sûr mais pas optimal. Un re-probe ciblé avant `resolve_job_url` ajouterait une latence/risque réseau sur le chemin critique du worker ; à évaluer en fonction du taux d'échec observé en prod avant d'investir. | `backend-developer` (worker), en coordination `sync-specialist` |
| **DL-PLEX-02** | Pas de sélection multi-versions par serveur (résolution/piste audio) | P2 | MVP retient le **meilleur** élément `Media[]` par item au sync (`plex_media_item` = 1 ligne par `(server_id, rating_key)`) — un PMS qui expose plusieurs qualités du même fichier n'en montre qu'une. Élargir `plex_media_item` à une ligne par élément `Media[]` change la clé primaire effective et la logique de dédup (`source_count` compterait des variantes, pas des serveurs) — refonte non-triviale, hors scope C7. | `db-migration-specialist` + `backend-developer` (effort dédié) |
| ~~**DL-PLEX-03**~~ **✅ RÉSOLU** (Vague W0, `/refacto` écran unifié) | Pas de NFO sidecar pour les jobs Plex | P2 | `download_nfo.render_plex_media_nfo(item)` (adaptateur `PlexMediaItem`→`PlexMovie`/`PlexEpisode`, réutilise `nfo_builder`) + `download_worker._write_sidecar_nfo` branche désormais sur `is_plex_server_id` (jobs Plex → `plex_media_item`, Xtream → `Media`). NFO minimal mais exploitable (titre/année/ids/genres M021/durée). `thumb_url` PMS-relatif jamais émis (nécessiterait le token). Best-effort inchangé. | — |
| **DL-PLEX-04** | Vignettes non proxifiées (`thumb_url` nécessite le token pour être résolu) | P2 | `PlexMediaItem.thumb_url` est stocké tel quel (chemin relatif PMS, sans token — volontaire, cf. PRD §9) ; aucun endpoint ne le sert en image directement car cela nécessiterait un proxy serveur qui attache le token per-serveur à la volée (nouvelle surface : quel serveur, quelle expiration de token, quel rate-limit) — pas nécessaire au MVP catalogue texte-only. | `backend-developer` (nouvel endpoint proxy, si demande produit) |
| **DL-PLEX-05** | Pas de protection CSRF sur `POST /admin/plex-downloads*` (sync/enqueue) | P2 (sécu) | **Même dette transverse existante `CR-S07`/`DL-03`** qui couvre déjà tout `/admin` (pas seulement Xtream) — ce nouvel onglet hérite du même gap, pas une régression propre à Plex. À corriger dans le même effort transverse `/admin` mentionné pour DL-03, pas séparément par onglet. | `security-reviewer` puis `backend-developer` (effort dédié `/admin`, même que DL-03) |
| **DL-PLEX-06** | Pas de métriques Prometheus dédiées à la sync catalogue Plex | P2 | Miroir de **F-103** (métriques download déjà trackées non livrées au MVP Xtream) : aucun `plexhub_plex_sync_*` (durée, serveurs joignables/total, items synchronisés). Le suivi actuel passe par `GET /admin/plex-downloads/sync/status` (polling HTMX) et `GET /api/admin/plex-downloads/servers` (JSON, `isReachable`/`lastSyncedAt`/`lastSyncError`), suffisant pour le MVP ; les métriques HTTP génériques (instrumentator) couvrent déjà les routes elles-mêmes. | `observability-analyst` / `backend-developer` (sprint suivant, avec F-103) |
| **DL-PLEX-07** | Serveur bloquant *aussi* le direct-play (transcode-only) | P3 | Le fallback download-désactivé (DL-PLEX-08, résolu) récupère les octets via l'URL direct-play du `Part.key`. Si un partage très verrouillé bloque **aussi** le direct-play (le client ne peut que transcoder), même l'URL sans `download=1` échoue (403/non-200) → job `failed`. Un vrai « download » supposerait de **capturer le flux HLS transcodé + remux ffmpeg** (dépendance ffmpeg, perte de qualité, sessions de transcodage côté PMS) — grosse feature, non justifiée tant qu'aucun serveur transcode-only n'est observé (l'utilisateur a confirmé avoir accès au direct-play). | `backend-developer` (effort dédié, si demande) |

**✅ Résolu — Écran de téléchargement UNIFIÉ Plex+Xtream (`/refacto` 2026-07-17, Vagues W0→W3, commits `8e3ab35`+`e44da7e`).** 3e onglet admin `/admin/unified-downloads` (« Téléchargements ») qui **fusionne** les deux catalogues par `unification_id` (clés id-based `imdb://`/`tmdb://` **byte-identiques** entre Xtream et Plex → un titre présent des deux côtés n'apparaît **qu'une fois**, badges Plex/Xtream + total sources ; fallbacks `title_`/`plexsrc://` jamais cross-mergés). Filtre **genre** sur les deux (migration **021** `plex_media_item.genres` + capture `Genre[].tag` au sync, W1). Les 2 onglets source-spécifiques restent **inchangés**. Service `unified_download_catalog_service` (fusion en mémoire bornée + flag `truncated`, secret-free). L'écran **browse-only délègue** l'enqueue : chaque carte lazy-load le sélecteur `/versions` **existant** de chaque origine (formulaires postant déjà vers `#downloads-queue` partagée ; worker routé par `is_plex_server_id`) → **aucune nouvelle logique enqueue/worker/queue**. NFO auto pour jobs Plex (W0, DL-PLEX-03 résolu). Review `code-reviewer` = **APPROVED**. Tests : `test_unified_download_catalog` + `test_admin_unified_downloads` + `test_migration_021` + NFO/api. **Follow-up P3 UDL-01** : la clé de contexte `total` du panneau file partagé écrase le total catalogue au 1ᵉʳ rendu pleine-page (corrigé **localement** sur l'onglet unifié via `catalogue_total` ; **subsiste, identique et pré-existant**, sur `admin_downloads.py:179` et `admin_plex_downloads.py:200` — à corriger d'un coup sur les 3, `backend-developer`).

**✅ Résolu — UDL-02 (`/refacto` 2026-07-17) — l'écran unifié était KO au 1er test (recherche/genre/type sans effet ; ouverture d'une carte bloquée sur « Chargement des sources… » ; aucune trace d'erreur serveur).** Cause 100 % **client-side htmx**, diagnostiquée **in-browser** (Chrome DevTools MCP + repro minimale htmx 1.9.12 servie en local, chaque hypothèse prouvée par le journal d'accès) : **(1) filtres morts** — le trigger `change from:#…-filters select, keyup changed … from:#…-filters input[…]` est **mistokenisé** par htmx dès qu'un sélecteur `from:` contient un ESPACE (combinateur descendant `#id select`), qui **drop** les triggers `change`/`keyup` (seul `submit`/Entrée survivait) ; **de plus `changed` sur un `<form>` ne se déclenche jamais** (pas de `.value`). **(2) sources bloquées** — l'ancien 2-sauts `<details hx-get>`→`/sources`→`hx-trigger="load"` imbriqué ne se déclenchait jamais car **htmx ne tire aucun auto-trigger (`load`/`revealed`/`intersect`) sur du contenu swappé dans une cible DESCENDANTE de l'élément porteur du `hx-get`** (`hx-target="find .sources-body"`) — vérifié : le même loader imbriqué fonctionne si la cible du swap parent est externe. Correctifs : (1) `hx-trigger="change, keyup delay:300ms, submit"` (bulle vers le form, sans `from:` ni `changed`), **appliqué aux 3 onglets download** (`unified`/`downloads`/`plex_downloads` — même bug latent, filtres réparés partout) ; (2) suppression du saut `/sources` — route + template `_unified_downloads_sources.html` + `get_group_availability`/`UnifiedGroupAvailability` **supprimés** ; les loaders `/versions` par origine sont désormais des **enfants statiques** du `<details>`, déclenchés par l'événement DOM réel `hx-trigger="toggle once from:closest details"` (ciblant self) → fiable, un round-trip de moins. Flux complet re-vérifié en navigateur (change filtre → refresh liste → ouvrir une carte de la liste swappée → les 2 loaders tirent leur `/versions`). Suite : **1168 passed, 3 skipped**, `ruff` vert, `/api/health` 200. Tests MAJ : `test_admin_unified_downloads` (loaders inline + auth) ; retrait des tests `/sources`/`get_group_availability` (chemin supprimé).

**✅ Résolu — DL-PLEX-08 (`/refacto` 2026-07-16) — téléchargement possible même si « Allow Downloads » est désactivé côté serveur partagé.** Cause : l'URL `{base_uri}{Part.key}?download=1&X-Plex-Token=…` est gardée par la permission download du partage → **403** quand le propriétaire l'a désactivée, et le worker marquait le job `failed` définitivement. Correctif : `plex_download_service.resolve_job_urls` renvoie désormais **deux** URLs candidates `[?download=1, direct-play sans download=1]` ; `download_service.download_to_disk` (nouveau param `fallback_urls`) **bascule sur l'URL direct-play sur un 403** (octets **identiques** au fichier original, Range-capable, gardée par la permission *lecture* seule que l'opérateur possède) **sans consommer le budget de retry**. `fallback_urls=[]` côté Xtream ⇒ 403 reste permanent (chemin **inchangé**, 0 régression sur les ~160 tests download). Secrets : URLs/token jamais loggés/persistés (ligne de log de bascule = string fixe). Tests : `tests/test_download_transfer.py::TestPlexDownloadDisabledFallback` (3) + `tests/test_plex_download_worker.py` (resolve_job_urls + wiring worker). Review `code-reviewer` = **APPROVED**. Réf §5.9 CLAUDE.md. Escalade transcode-only = DL-PLEX-07 ci-dessus.

DoD de ces follow-ups (quand ils seront pris) : même gate que tout ticket de ce board (§ Definition of Done ci-dessus)
+ test de garde dédié à chaque finding.

## Extension « Télécharger » Xtream — granularité épisode/saison + taille (tickets X1a→X4)

Parité avec « Télécharger Plex » côté Xtream : `enqueue_selection` gagne les scopes `seasons`/`episodes`
avec **source par unité** (chaque saison/épisode d'un compte différent), UI miroir (route `/episodes`,
sélecteurs de source par saison/épisode), et **affichage de la taille** des médias. Taille exacte via la
nouvelle colonne `media.file_size` (**migration 020**) peuplée par le `Content-Length` du HEAD du
health-check (aucune requête réseau ajoutée), estimation `bitrate×durée` en fallback (préfixe « ~ »).
Scopes existants `movie`/`series_all`/`series_seasons` inchangés (non-régression, 84 tests verts).

| ID | Titre | Sévérité | Pourquoi non corrigé maintenant | Owner suggéré |
|---|---|---|---|---|
| **XD-01** | Taille se remplit progressivement (lazy) au fil des cycles de health-check | P3 (accepté) | Choix produit validé : la taille exacte n'existe pas dans le catalogue Xtream ni l'API provider — elle est capturée du `Content-Length` du HEAD que le health-check fait déjà (cron `hour=2` + validation pipeline, échantillon `ORDER BY random()` CR-P06). Un média non encore health-checké affiche `—` (ou une estimation `~` si `media_parts` porte un bitrate). Alternative « HEAD à la demande au clic » écartée (latence UI + charge provider, lourde pour la vue par-épisode M×N). | `sync-specialist` (si un préchauffage ciblé de la taille devient nécessaire) |
| **XD-02** | Estimation `bitrate×durée` indisponible pour les films | P3 | `media_parts` est `[]` pour les films Xtream (`sync_worker`), donc pas de bitrate → un film sans `file_size` capturé affiche `—` (pas d'estimation `~`). Les épisodes ont un bitrate dans `media_parts` (estimation possible). Capturer un bitrate film au sync (`vod_info.bit_rate`) serait un petit ajout `sync_worker` si l'estimation film devient souhaitée. | `sync-specialist` (si estimation film demandée) |
| **XD-03** | Pas de protection CSRF sur les nouveaux `POST /admin/downloads` (seasons/episodes) | P2 (sécu) | **Même dette transverse `CR-S07`/`DL-03`** couvrant tout `/admin` — la route POST enqueue existait déjà, ce ticket n'en change que le format de champs. Aucune régression propre. À corriger dans l'effort transverse `/admin` (voir DL-03). | `security-reviewer` puis `backend-developer` (effort dédié `/admin`, même que DL-03) |

DoD de ces follow-ups (quand ils seront pris) : même gate que tout ticket de ce board (§ Definition of Done ci-dessus)
+ test de garde dédié à chaque finding.
</content>
