> 🕒 **À JOUR AU : 2026-07-16 (HEAD develop post-`ec6abb1`, release **v1.3.1**).** Autorité de vérité du backend PlexHub. Base cartographie = `/refresh-context` @ `1879f83` (a0-cartographer, §2/§3/§5/§7/§9/§10 relues `fichier:ligne`), puis **campagne de remédiation `/fix-cleanroom` `1879f83`→`3833349`** (voir clause dédiée ci-dessous — modules §2 déplacés/ajoutés, ré-vérifier au besoin). Base de référence : audit clean-room indépendant `docs/audit/cleanroom-2026-07-11/` (56 findings, recoupés au code). Suite `pytest` = **1135 passed + 3 skipped** (couverture **76.07 %**, gate `--cov-fail-under=70`, `ruff check` vert ; post-`5c7fdcc` : **+6 = `/refacto` fallback Plex download-désactivé** voir §5.9 ; **+8/−3-skip = fix caractères illégaux NTFS** — `download_service._sanitize_segment` remappe désormais `:`→` - ` et supprime `* ? " < > |` via le helper public `remap_ntfs_illegal_chars`, pour que les fichiers écrits sur volume Linux ne soient pas mutilés en noms 8.3 par Samba côté Windows ; script de reprise `app/scripts/rename_download_illegal_chars.py` dry-run par défaut ; les 3 tests fs de rename sont POSIX-only, skip sur Windows) — dont les tests de la feature « Télécharger » (Xtream, confinement/security/service/transfer/worker + **granularité épisode/saison** `tests/test_download_granular.py` + **UI** `tests/test_admin_downloads.py`) **et** les tests de la feature « Télécharger Plex » (C1→C7, catalogue/enqueue/admin UI/JSON mirror, dont `tests/test_plex_downloads_json.py`, 19 tests). **Remédiation `/fix-cleanroom` quasi complète (Rounds 1-2 + Vagues A/B/C1/C2/D, commits `e465433`→`3833349`)** : **~47 findings résolus** (P0 CR-P01 + les P1 flux/perf/tests + refactos moyens A01/A02/A04/A06/C10 + conventions C03/C04/C05 + tests T03-T09), log complet `docs/audit/cleanroom-2026-07-11/README.md`. **Nouveaux services** (refacto A01/A02) : `account_service`, `live_service`, `plex_generation_service` (orchestration Plex-gen unifiée — `sync.py` n'importe plus `app.main`), `xtream_credentials`. **GARDÉS (effort dédié) :** god-files A03 (`sync_worker`/`ai`/`nfo_import`) + A05 (`main.py`) ; sécurité S02/S04/S05/S07/S08 + gate `verify_master_key` sur `/plex/generate`. **⚠️ Défaut découvert** : `commit_with_retry` sur le chemin requête (même session) n'attrape pas `PendingRollbackError` sur vrai lock → résilience partielle (atténué par `busy_timeout=60 s`), correctif = factory `run_with_retry` (effort dédié). **`/refacto` perf résiduels `3833349`→`c3024e3`** (commits `ba6689e`/`b1f5ed6`/`c3024e3`, ADR `docs/architecture/adr/0001-unified-perf-residuals.md`) : **CR-P07 RÉSOLU** (sérialisation single-pass sur les 6 endpoints liste via `_single_pass_json`, `response_model=` gardé pour l'OpenAPI, sortie octet-identique) ; **CR-P04 RÉSOLU** (curseur keyset **optionnel/additif** sur `/movies|shows|episodes` bruts, tri récence, offset conservé, `next_cursor` en réponse) ; **CR-P01 (P0) RÉSOLU** (table dénormalisée `media_group`+`media_group_member`, **migration 017**, builder `unified_group_service.rebuild_all` en fin de pipeline → browse **non-filtré** paginé côté SQL O(page) puis ré-agrégé par page ; **fallback live** si snapshot vide ou requête filtrée). **Migration courante = 019** (015 index `media`, 016 chiffrement Xtream, 017 snapshot `media_group`, 018 tables `download_job`/`download_batch` — feature « Télécharger » — et **019 tables `plex_server`/`plex_media_item`/`plex_sync_status`** — feature « Télécharger Plex » ; **020 colonne `media.file_size`** — taille de fichier (octets) capturée depuis le `Content-Length` du HEAD que fait déjà le health-check, affichée dans les sélecteurs de download Xtream, voir blocs dédiés ci-dessous). L'ancien P0 « CI réellement ROUGE » reste **RÉSOLU**.
> **🆕 Feature « Télécharger » — téléchargement PHYSIQUE de médias — MERGÉE (`c440717`→`9361941`, 8 commits ; PRD `docs/10-prd-media-download.md`, spec `docs/20-impl-media-download.md`, ADR `docs/architecture/adr/0002-media-download-writes-to-disk.md`, board `docs/31-board.md`)** : **PREMIÈRE capacité qui écrit les octets sur disque** (le reste du catalogue reste référence-only `.strm` — inchangé). Onglet admin **« Télécharger »** (`/admin/downloads`, HTMX, Basic Auth, `app/api/admin_downloads.py` + templates `admin/downloads.html`+`_downloads_{list,versions,queue}.html`, lien nav `base.html`) : liste films+séries unifiés (réutilise `media_service.get_unified_group`), l'opérateur choisit une **version** (`serverId:ratingKey`) ou une **série complète** (`scope=series_all` → 1 job/épisode, toutes saisons), lance, suit progression/état. Miroir **JSON lecture seule** `GET /api/admin/downloads` (garde `verify_master_key`, `app/api/downloads.py`). **File persistante** : tables `download_job`/`download_batch` (**migration 018**, entités ORM `models/database.py`, schémas Pydantic `DownloadJobResponse`/`DownloadJobListResponse`/`DownloadEnqueueRequest`) ; état `queued|running|completed|failed|canceled` + progression bytes/%, **aucune colonne URL/credential**. Service `app/services/download_service.py` : `enqueue_selection`/`list_jobs`/`cancel_job`/`retry_job`/`clear_finished` (writers via `run_with_retry`), `compute_dest_path`+`resolve_confined` (**confinement realpath sous `DOWNLOAD_DIR`, F-007** — chemin 100 % dérivé serveur, jamais client, contraste avec la dette CR-S01 `outputDir` de `plex.py`), `download_to_disk` (GET streaming httpx, **`follow_redirects=False`** anti-SSRF, reprise `Range` via `.part` + `os.replace` atomique), `check_free_disk_space` (préflight `DOWNLOAD_MIN_FREE_DISK_MB`). Worker `app/workers/download_worker.py` **master-only** (`run_drain_loop` démarré sous `if is_master:` dans le lifespan `main.py`, `reap_orphans` au boot `running`→`queued`, concurrence `DOWNLOAD_CONCURRENCY` défaut 1, back-off transitoire **hors** sémaphore, boucle de drain enveloppée `try/except` = résiliente aux erreurs DB non-lock). Config (`config.py`) : `DOWNLOAD_DIR` **dédié (≠ `PLEX_LIBRARY_DIR`)** — **feature no-op si vide** ; `DOWNLOAD_CONCURRENCY`/`DOWNLOAD_CHUNK_BYTES`/`DOWNLOAD_MAX_RETRIES`/`DOWNLOAD_MIN_FREE_DISK_MB`/`DOWNLOAD_POLL_INTERVAL`/timeouts connect+read. **Sécu** : URL Xtream (user/pass) **JAMAIS persistée/loggée/renvoyée** (re-dérivée au worker via `stream_service.build_stream_url`, `raise ... from None`). Review qualité **OK** (après remédiation `9361941`), sécu **SHIP-avec-réserves**. **Follow-ups P2/P3** (board) : DL-03 CSRF admin (dette transverse CR-S07), DL-04/05 (chemin Windows/TOCTOU symlink — non exploitables sur cible Linux/Docker), F-103 métriques Prometheus download (P1 non livré au MVP). **Depuis (commits `aafb7fc`→`b92865a`, hors périmètre C1-C7 Plex) : la feature Xtream a gagné (1) un suivi de redirection provider→CDN vetté anti-SSRF (`DOWNLOAD_MAX_REDIRECTS`, défaut 5), (2) un sidecar `.nfo` best-effort post-complétion (`app/services/download_nfo.render_media_nfo` + `download_worker._write_sidecar_nfo`, lit la table `Media` par `(server_id, rating_key)` — best-effort, jamais fatal), (3) une sélection par saison (`scope=series_seasons` en plus de `series_all`).** **Extension `15f51c3`→`5c7fdcc` (parité avec « Télécharger Plex ») : granularité complète — `enqueue_selection` gagne les scopes `seasons` (`season_picks=(season, server_id, series_rating_key)`) et `episodes` (`episode_picks=(server_id, episode_rating_key)`) avec **source par unité** (chaque saison/épisode peut venir d'un compte différent), en plus des `movie`/`series_all`/`series_seasons` existants (INCHANGÉS). L'UI (`admin_downloads.py` : route `GET /{type}/{id:path}/episodes?season=N`, POST enqueue en `source`/`season_pick`/`episode_pick`, templates `_downloads_versions.html` réécrit + `_downloads_episodes.html`) miroir de l'onglet Plex : sélecteurs de source par saison/épisode. **Taille des médias affichée** : nouvelle colonne `Media.file_size` (**migration 020**, nullable) peuplée par le `Content-Length` du HEAD du health-check (`_check_one` → 4-tuple, écriture conditionnelle sur `_run_health_check_batch`+`_run_pipeline_validation_impl`, jamais d'écrasement par NULL, aucune requête réseau ajoutée) ; helpers de lecture `download_service.list_series_seasons_with_sources`/`list_series_episodes_with_sources` (sources cross-comptes avec taille) + `estimate_media_size(row)` = taille exacte (`file_size`) sinon estimation `bitrate×durée` depuis `media_parts` (préfixe « ~ » dans l'UI), sinon inconnue. Le worker/dérivation d'URL Xtream restent inchangés (aucune nouvelle surface secret).**
> **🆕 Feature « Télécharger Plex » — téléchargement physique depuis des serveurs Plex Media Server PARTAGÉS (via plex.tv) — MERGÉE (`caededa`→`b92865a`, tickets C1→C7 ; PRD `docs/10-prd-plex-download.md`, board `docs/31-board.md`)** : **miroir** de la feature « Télécharger » ci-dessus, mais avec une **source de catalogue distincte** — au lieu du catalogue Xtream déjà synchronisé (`media`), cette feature découvre et parcourt les **PMS accessibles via le token plex.tv de l'opérateur** (possédés OU partagés par un tiers). Onglet admin **« Télécharger Plex »** (`/admin/plex-downloads`, HTMX, Basic Auth, `app/api/admin_plex_downloads.py` + 5 templates `admin/plex_downloads.html`+`_plex_downloads_{list,versions,episodes}.html`+`_plex_sync_status.html`, lien nav `base.html`). Miroir **JSON lecture seule** `GET /api/admin/plex-downloads/{servers,catalog,catalog/{type}/{unification_id}}` (garde `verify_master_key`, `app/api/plex_downloads.py`, ticket **C7**). **Tables catalogue dédiées, isolées de `media`** (**migration 019**) : `plex_server` (serveur PMS connu, `access_token` **chiffré Fernet** — `EncryptedString`, jamais exposé ; `base_uri` stocké **en clair** — il ne porte PAS le token — mais exclu des réponses API/HTML pour ne pas divulguer l'hôte/IP du propriétaire), `plex_media_item` (mirror léger movie/show/episode par serveur, clé `(server_id, rating_key)`, `unification_id` peuplé au sync), `plex_sync_status` (singleton d'état de sync, claim `idle→running` conditionnel). Services : `plex_api_service` (client plex.tv resources + probe PMS), `plex_sync_service` (`run_full_sync(session_factory) -> PlexSyncReport` — discover→probe→catalogue-sync mark-and-sweep→dédup, claim/`already_running`/`reap_sync_status` au boot master), `plex_catalog_service` (lectures pures : `list_unified`/`get_group`/`list_seasons_with_sources`/`list_episodes_with_sources`/`list_servers`/`get_sync_status` — dédup = simple `GROUP BY unification_id` SQL, **pas** de convergence en mémoire comme `aggregation_service` côté Xtream, car `unification_id` est déjà résolu au sync), `plex_download_service` (`enqueue_plex_selection` scope `movie`/`series_all`/`seasons`/`episodes`, `resolve_job_url` re-dérive l'URL au worker). **Queue `download_job` PARTAGÉE avec la feature Xtream** — aucune nouvelle table de file : discriminant `download_job.server_id` préfixé `plex_<clientIdentifier>` (`utils/server_id.build_plex_server_id`/`parse_plex_server_id`/`is_plex_server_id`, ≠ préfixe Xtream `xtream_`, jamais de collision) ; `download_worker` route sur `is_plex_server_id(job.server_id)` pour dispatcher vers `plex_download_service.resolve_job_url` au lieu de `stream_service.build_stream_url` — **aucune duplication de worker**. **Dédup priorité imdb > tmdb > `plexsrc://`** (jamais titre+année seul, contrairement à la convergence `aggregation_service` côté Xtream — `plex_media_item.unification_id` est peuplé déterministe au sync). Config (`config.py`) : `PLEX_ACCOUNT_TOKEN` (défaut `""` = **feature no-op**), `PLEX_CLIENT_IDENTIFIER` (auto-généré + persisté sur disque si absent), `PLEX_PROBE_TIMEOUT` (défaut 5 s), `PLEX_SYNC_INTERVAL_HOURS` (défaut `0` = cron désactivé — sync manuelle uniquement via le bouton admin ; **ticket C7** câble le job APScheduler optionnel `plex_catalogue_sync`, `main.py`, gardé `if settings.PLEX_ACCOUNT_TOKEN and settings.PLEX_SYNC_INTERVAL_HOURS > 0:`, sous le même `if is_master:` que le reste du scheduler — **aucun job enregistré** si l'une des deux conditions manque, pas seulement un skip runtime). **Sécu** : `access_token` par serveur (secret plex.tv) **chiffré au repos (Fernet, même convention que `XtreamAccount.password` CR-S03), jamais loggé, jamais renvoyé** ; `base_uri` (non secret, sans token) exclu des réponses API/HTML ; l'URL de download est re-dérivée au worker (`resolve_job_urls` → couple download+direct-play, **fallback 403 si download désactivé**, §5.9), jamais persistée ; confinement d'écriture (F-007) **réutilisé tel quel** (même `DOWNLOAD_DIR`/`resolve_confined` que Xtream, aucune nouvelle surface). **Isolation totale** de la génération de bibliothèque `.strm`/NFO existante — `plex_media_item` n'est **jamais** lu par `DatabaseSource`/`PlexLibraryGenerator` (les deux sous-systèmes partagent le mot « Plex » dans leur nom mais sont disjoints : l'un génère une bibliothèque *pour* Plex à partir du catalogue Xtream, l'autre télécharge *depuis* de vrais serveurs Plex). Le sidecar `.nfo` best-effort de la feature Xtream (ci-dessus) **ne couvre pas** les jobs Plex (`_load_media` interroge `Media`, pas `PlexMediaItem` — silencieusement sauté). **Follow-ups P2/P3** (board, section dédiée « Télécharger Plex ») : re-probe auto sur échec de connexion, multi-versions/serveur (MVP = meilleur `Media[]` retenu), NFO sidecar `plex_media_item`, vignettes proxifiées (`thumb_url` nécessite le token), CSRF admin (même dette transverse CR-S07/DL-03), métriques Prometheus sync Plex.
> **⚠️ AUTH FAIL-CLOSED SUR TOUTE L'API (confirmée EMPIRIQUEMENT cette session : `/api/media/movies` sans clé → 401 ; `/health` sans clé → 200)** : l'ancien modèle « seuls `/api/ai` + `/api/tv-auth/approve` authentifiés ; catalogue/sync/plex/admin OUVERTS » est **CADUC** (l'ancienne dette CR-S01 P0 « auth incomplète » est **RÉSOLUE**). `_guard = [Depends(verify_backend_secret)]` (`main.py:396`) garde `accounts`/`categories`/`live`/`media`/`stream`/`sync`/`plex` (`main.py:399-405`) ; `verify_backend_secret` (`deps.py:59`) exige `X-API-Key` = secret maître `AI_API_KEY` **OU** une clé par-utilisateur active de la table `api_keys` (comparaison temps-constant `secrets.compare_digest` `deps.py:46`) → **401** sinon. Restent **publics** : `health` (`main.py:398`) et `tv_auth` start/status/complete (`main.py:408` ; seul `/approve` gardé — `verify_pairing_api_key` est un **alias** de `verify_backend_secret`, `tv_auth.py:40,252`). `/api/ai` gardé par `verify_api_key` (`ai.py:52-56`). Gestion des clés **`/api/admin/keys`** gardée par `verify_master_key` (secret maître SEUL → une clé par-utilisateur ne peut pas en créer/révoquer d'autres, `api_keys.py:20-23`, `deps.py:88`). Admin + `GET /docs`/`/openapi.json` derrière **Basic Auth** `verify_admin_basic_auth` (`deps.py:108` ; `main.py:412,423,428` ; `ADMIN_USERNAME`/`ADMIN_PASSWORD` `config.py:28-29`, **503 si `ADMIN_PASSWORD` vide** = fail-closed par défaut). Côté app Android : `BackendApiKeyInterceptor` attache déjà `X-API-Key` (il faut juste saisir la clé dans l'app).
> **Delta `5827282`→`1879f83` (~50 commits v1.1.0→v1.1.5, vérifié ligne-à-ligne)** : (1) **gestion multi-clés API** (`7538b3b`) — module `app/api/api_keys.py` (router monté `main.py:438`, préfixe `/api/admin/keys`) + service `app/services/api_key_service.py` (`resolve`/mint/revoke/expiry ; clés stockées en **digest SHA-256**, plaintext `phk_…` rendu une seule fois) + entité ORM `ApiKey` (`models/database.py:360`, table `api_keys` **créée par `Base.metadata.create_all` `database.py:92`, PAS de migration numérotée**). (2) **Nouveaux endpoints media** (`b21f7e2`) : `GET /api/media/{movies,shows}/unified?unification_id=` → un seul groupe (total=1/0) via `media_service.get_unified_group` (`media_service.py:149`, même passe `aggregate_movies`/`_converge`), additif/rétrocompatible, `tests/test_unified_by_id.py` — ⚠️ sous-rapporte les jumelles convergées (CR-F05, §10) + scaffold **cloudflared** commenté (`docker-compose.yml`/`.env.example`, inerte). (3) import NFO riche tinyMM **MERGÉ** (M014) + enrichment peuple les colonnes NFO (`0ff8caa`) + age certification (`a1325d6`) ; convergence d'identité `_converge` **MERGÉE** (`2595dfb`) + `dedup_resolved_twins` one-shot (`ffa636c`) ; validation clamp concurrence (`8873f58`), plex-gen labels de version déterministes (`82620fd`), DB `busy_timeout` 60s sur pool (`ef49b32`, `database.py:15`), health-check sérialisé vs pipeline (`b832813`). `APP_VERSION="1.1.5"` (`main.py:17`).
> **Deltas majeurs depuis le dernier refresh (`4887ee6`) :**
> 1. **Pack IA « feature pack » MERGÉ ET LIVE** (`8759a51`, ancêtre de HEAD) — désormais **pleinement cartographié** (§2/§3/§5/§9/§10), ne plus le traiter comme absent ou sous-documenté. Le router `/api/ai` expose **13 endpoints** (`ai.py`, 1228 lignes) : recos vectorielles (`/rank`, `/rank-multi` + **explications « why recommended »** `explain=true`, `_explain_items` `ai.py:220`), **recherche sémantique NL** (`POST /search` `ai.py:499`), **assistant catalogue RAG** (`POST /assistant` `ai.py:623`), **blurb FR + mood tags** (`POST /blurb` `ai.py:1084` / `GET /blurb/{tmdb_id}` `ai.py:1201`), **traduction de sous-titres SRT/VTT** (`POST /subtitles/translate` `ai.py:956`), LLM Ollama (`/describe` `ai.py:863`, `/chat` SSE `ai.py:886`, `/llm/status` `ai.py:912`), rebuild/status embeddings. Nouveaux services : **`subtitle_service.py`** (parse/traduit/re-sérialise SRT+VTT via gemma4) + `ollama_service.py`. Nouvelles tables : `ai_subtitle_cache` (**M011**), `ai_media_blurb` (**M012**).
> 2. **Tagging films adultes (+18 / XXX)** (`331f3c7`) : films d'une catégorie Xtream « adulte » (nom matchant `config.ADULT_CATEGORY_KEYWORDS` défaut `adult,xxx,+18,18+,porn,porno,x-rated`, OU id dans `config.ADULT_CATEGORY_IDS`) → `Media.is_adult` (colonne, **M013**), `content_rating` forcé à `config.ADULT_CONTENT_RATING` (défaut `"XXX"` → NFO `<mpaa>` + API), préfixe `[XXX] ` (jamais stocké en DB) appliqué à l'**API et désormais à la bibliothèque Plex/Jellyfin générée** — cf. delta 6. Idempotent par sync via `category_service.update_media_adult_flags()` (`category_service.py:410`, appelé `sync_worker.py:1330`). **Films (`type='movie'`) uniquement.** Champ `isAdult` sur `MediaResponse`/`UnifiedMediaResponse`/`LiveChannelResponse`.
> 3. **Fix import NFO arbre plat** (`9a574a9`) : `nfo_import_service.import_nfo` lit `<root>/Films|Series` + un `.plex_mapping.json` **unique** keyé `server_id:rating_key` (plus de boucle `root/<account_id>`) — cohérent avec la génération à plat.
> 4. **Stack épinglée** (`a8a0ce7`) : `fastapi>=0.115,<0.116` + `prometheus-fastapi-instrumentator>=7.0,<8` **bornés ensemble** (FastAPI 0.116+ émet des `_IncludedRouter` que le route-walker de l'instrumentator 8.x ne sait pas gérer → tous les `/api/*` en 500 ; outage corrigé). Cf. §10.
> 5. **Versioning & health alignés** : `APP_VERSION="1.1.5"` (`main.py:17`, release v1.1.5). `GET /api/health` renvoie désormais la **version live** (`request.app.version` = APP_VERSION, `health.py:33`) — l'ancienne dette « `1.0.0` codé en dur » (CR-C05) est **RÉSOLUE** (`ebcd051`). OpenAPI ⇆ health cohérents.
> 6. **Préfixe `[XXX] ` étendu à la bibliothèque générée** (ce commit, `/refacto`) : les films `is_adult` portent désormais le tag `[XXX] ` sur le **nom de dossier**, le **nom des fichiers `.strm`** ET le **`<title>` du `movie.nfo`** — plus seulement dans l'API. Posé **après** la résolution de nom (`generator._resolve_movie_names`, car `parse_title_year_and_suffix` strippe sinon un crochet `[...]` de tête `string_normalizer.py:6,214`) + dans `nfo_builder.build_movie_nfo`, via le helper idempotent `apply_adult_prefix` (`schemas.py:16`). `PlexMovie` porte un champ **`is_adult`** (`models.py`, alimenté `source.py` ⟵ `best.is_adult`). **Films uniquement**, jamais persisté en DB, **aucune migration** (colonne M013 déjà présente).
> Acquis antérieurs toujours valides : LLM Ollama/gemma4, dédup unifiée `unification_id` (`aggregation_service`, 2 consommateurs), scraping TMDB v2 (cache persistant `tmdb_scrape_cache`, M010, `plexhub_tmdb_match_total`).
> **L'ancien P0 « CI réellement ROUGE » est RÉSOLU** (`aed840f`) — voir §4 + §10.
> Maintenance (anti-dérive, OBLIGATOIRE) : tout commit qui touche les modules (§2), le schéma SQLite / les migrations, les flux (§5) ou les conventions (§3) **met à jour le bandeau ci-dessus (date + HEAD) + la section concernée dans le même commit**, OU lance **`/sync-context`** avant de clôturer le workflow. Le détecteur SessionStart (`.claude/hooks/session-start.js`) signale la dérive. En cas de doute sur un fait, **vérifier dans le code** (`fichier:ligne`).

# CLAUDE.md — PlexHub Backend

## §1. Résumé
Backend **FastAPI** (Python, async) du stack PlexHub : miroir de bibliothèques **Xtream-IPTV** (VOD + séries + chaînes Live + EPG), enrichissement **TMDB**, validation de flux, génération de **bibliothèque Plex/Jellyfin** (NFO + arborescence + .strm/images), **recommandations IA** (embeddings + recherche vectorielle), **génération LLM** (Ollama/gemma4 : présentations + chat) et **appairage TV** device-flow. Consommé par l'app Android `PlexHubTV`. Le backend ne stocke **aucun historique utilisateur côté IA** : `ai_tmdb_cache` + `ai_embeddings` sont clés sur `tmdb_id`. Cible de déploiement **Docker/Linux** : l'élection master-worker s'appuie sur `fcntl.flock` (POSIX, `app/main.py:226-227`).

## §2. Modules (`app/`)
- **`main.py`** — app FastAPI + `lifespan` : `init_db()`, **élection master-worker** via `fcntl.flock` sur `DATA_DIR/server_start.lock` (`main.py:226-227`), planificateur APScheduler (master seul), auto-provision Xtream si env, génération auto de bibliothèque Plex/Jellyfin **unifiée multi-comptes** (`_auto_generate_plex_library`, `main.py:77-118`), middlewares CORS/GZip/RequestId (`main.py:382-390`), montage des routers (`main.py:398-438`), instrumentation Prometheus (`setup_instrumentator`, `main.py:442`). **`APP_VERSION = "1.1.5"` (`main.py:17`)** — passé à `FastAPI(version=...)` (`main.py:372`) ET renvoyé par `GET /api/health` via `request.app.version` (`health.py:33`, plus de valeur codée en dur). Définit `SafeRotatingFileHandler` (`main.py:22-35`). ⚠️ `main.py` = 442 LOC concentrant logging + 3 coroutines métier (`_auto_generate_plex_library` `:77-118`, `_auto_provision_xtream_account`, `_cleanup_stale_epg`) + lifespan/scheduler (dette CR-A05).
- **`config.py`** — classe `Settings` **maison** lisant l'env via `os.getenv` + `python-dotenv` (`config.py:6,21`), **pas** `BaseSettings`/pydantic-settings (la dépendance est listée mais inutilisée ici). Clés : `TMDB_API_KEY`, `AI_API_KEY`, `TV_AUTH_ENCRYPTION_KEY` (Fernet, `config.py:29`), `AI_EMBED_MODEL`/`AI_EMBED_CACHE_DIR`, **`OLLAMA_URL` (défaut `http://khoj-ollama:11434`) / `OLLAMA_MODEL` (défaut `gemma4:e4b`)** (`config.py:60-62`, loggés `config.py:84`), `DATA_DIR`/`LOG_DIR`, knobs sync/validation/backup, `CORS_ORIGINS` (**défaut `*`**, `config.py:55-57`), `TMDB_LANGUAGE` (défaut `fr-FR`, `config.py:58`), auto-provision Xtream (`config.py:64-68`), **tagging adulte** : `ADULT_CONTENT_RATING` (défaut `"XXX"`), `ADULT_CATEGORY_KEYWORDS` (défaut `adult,xxx,+18,18+,porn,porno,x-rated`), `ADULT_CATEGORY_IDS` (override par id).
- **`api/` (12+ routers, 67+ endpoints — compte pré-feature « Télécharger »/« Télécharger Plex », non recompté depuis, cf. bandeau pour l'état pytest/OpenAPI courant)** — `health` (`/api/health`, **public**, `main.py:398`), `accounts` (`/api/accounts`), `categories` (`/api/accounts/{id}/categories`), `live` (`/api/live`), `media` (`/api/media` ; **endpoints agrégés dédupliqués pour l'app** : `GET /api/media/movies/unified` (`media.py:150`), `/shows/unified` (`media.py:217`), `/episodes/unified?unification_id=…` (`media.py:278`) → 1 entrée par titre + `versions[]`, en plus des endpoints bruts par-ligne existants), `stream` (`/api/stream/{rating_key}`), `sync` (`/api/sync`), `plex` (`/api/plex`, génération `.strm`/NFO — **sans rapport** avec la feature « Télécharger Plex » ci-dessous malgré le nom), `tv_auth` (préfixe `/tv-auth` → `/api/tv-auth`), **`ai`** (définit lui-même `prefix="/api/ai"`, dépendance module `verify_api_key`, `ai.py:52-56` ; **13 endpoints**, cf. §5.5/§5.7/§5.8), **`api_keys`** (préfixe `/api/admin/keys`, module-level `verify_master_key`, monté `main.py:438`), `admin` (UI HTML/HTMX, préfixe `/admin`, **monté sans `/api`**, `main.py:412`). **Feature « Télécharger » (Xtream, physique)** : `admin_downloads` (HTMX `/admin/downloads`, Basic Auth au mount) + `downloads` (Pattern C, self-prefixé `/api/admin/downloads`, module-level `verify_master_key` — miroir JSON lecture). **Feature « Télécharger Plex » (C1→C7)** : `admin_plex_downloads` (HTMX `/admin/plex-downloads`, Basic Auth au mount, même convention que `admin_downloads`) + **`plex_downloads`** (Pattern C, self-prefixé `/api/admin/plex-downloads`, module-level `verify_master_key` — miroir JSON lecture-seule `servers`/`catalog`/`catalog/{type}/{unification_id}`, `app/api/plex_downloads.py`, ticket **C7**). ⚠️ **3 conventions de montage cohabitent** (guard au mount vs guard module-level) → aucune garantie structurelle centrale que tout `/api/*` est authentifié (dette CR-A04). `deps.py` = **gardes d'auth** (auth fail-closed sur tout `/api/*`, voir bandeau) : `verify_backend_secret` (JSON API entière — accounts/media/stream/…, `deps.py:59`), `verify_api_key` (`/api/ai` : même auth + check sqlite-vec 503, `deps.py:71`), `verify_master_key` (gestion des clés + les 2 miroirs JSON download, secret maître seul, `deps.py:88`), `verify_admin_basic_auth` (UI `/admin` + `/docs`/`/openapi.json`, Basic Auth constant-time, `deps.py:108`).
- **`services/`** — logique métier : `xtream_service` (client `player_api.php`), `tmdb_service` (singleton, recherche/détails/`find_by_imdb_id`, **scoring ScraperMatcher `_best_match` + tie-break résumé `_summary_sim`**, `search_multi`, caches TTL mémoire), **`scrape_cache_service`** (cache de scrape **persistant** `tmdb_scrape_cache` → ne plus rappeler TMDB pour un même titre ; TTL match 30 j / négatif 3 j, `scrape_cache_service.py:25-26`), `media_service` (+ **agrégation unifiée API** : `get_unified_list`/`get_unified_episodes`/`account_labels`), **`aggregation_service`** (regroupement **partagé** par `unification_id`, fonctions pures sur `Media` — `group_key`/`best_row`/`canonical_title_year`/`version_label`/`dedup_labels`/`aggregate_movies`/`aggregate_series` ; consommé par l'API REST **et** le générateur Plex pour une dédup identique ; **+ convergence** `_merge_by_shared_ids` (Passe A : fusionne les lignes d'un même titre dont l'`unification_id` **diverge** — `imdb://` vs `tmdb://` — dès qu'elles partagent un `imdb_id`/`tmdb_id` physique, **sans risque d'homonyme**) puis `_absorb_title_groups` (Passe B : absorbe une jumelle restée `title_…` dans le groupe id-based de même titre+année, **gardée contre les titres dégénérés** type non-latin → `title__YYYY`), appliquées via `_converge` avant la construction des groupes), `category_service` (filtrage par catégorie + **`update_media_adult_flags`** `category_service.py:410` : reset puis flag `Media.is_adult=True` & force `content_rating=ADULT_CONTENT_RATING` pour les films d'une catégorie adulte — nom matchant `_is_adult_category_name` ou id explicite —, **films `type='movie'` uniquement**, idempotent & rétroactif par sync), `stream_service` (`build_stream_url`/`parse_rating_key`), `nfo_import_service` (import **métadonnées riches** depuis `.nfo` tinyMediaManager ; **lit l'arbre plat `<root>/Films|Series` + `.plex_mapping.json` unique keyé `server_id:rating_key`** ; réconciliation déterministe ; `_FIELD_MAP` pilote l'écriture fill-missing-only (overwrite opt-in) sur **~26 colonnes** : ids `imdb/tmdb/tvdb/wikidata`, `summary/year/duration/content_rating/genres/cast`, **notes par source `imdb_rating`+`imdb_votes` / `tmdb_rating`+`tmdb_votes`** (+ `scraped_rating` best-pick IMDb-priorité, `audience_rating`=note TMDB), `original_title/tagline/premiered/status/studio/country`, `resolved_thumb_url`/`resolved_art_url` (poster/fanart), `cast_json` structuré, `title_sortable` ; **recompute `display_rating`** = `COALESCE(scraped, audience, rating)` à chaque écriture de note, **+ recompute `unification_id`/`history_group_key`** sur (ré)écriture d'id/année), **`embedding_service`** (fastembed lazy singleton + L2-normalize + centroïde ; `embed_passages`/`embed_query`/`weighted_centroid`, `DEFAULT_MODEL_NAME = paraphrase-multilingual-MiniLM-L12-v2`, `EMBEDDING_DIM=384`, `embedding_service.py:26-27`), **`recommendation_service`** (logique pure : `load_cached_vectors`/`hydrate_misses` (cap 20)/`cosine_rank` ; **+ `semantic_search`/`semantic_search_with_overview`** KNN sqlite-vec `vec0 MATCH … AND k=:k` (`recommendation_service.py:211,284`) **+ `generate_blurb`** synopsis FR + tags JSON via gemma4, `recommendation_service.py:403`), **`ollama_service`** (client httpx async vers l'API Ollama : `generate`/`chat`/`stream_generate`/`is_healthy` ; timeouts `_GENERATE_TIMEOUT=120s` / `_STATUS_TIMEOUT=5s`, `ollama_service.py:21-22` ; **aucun client SDK dédié — `AsyncClient` httpx éphémère par appel**), **`subtitle_service`** (NOUVEAU : parse SRT/VTT, traduit cue-par-cue par chunks concurrents via gemma4, re-sérialise ; exceptions `SubtitleFormatError`→422 / `SubtitleTooLargeError`→413 ; sentinelle de saut de ligne intra-cue ` ⏎ ` ; fallback texte d'origine si mismatch de comptage, `subtitle_service.py:461`), **`unified_group_service`** (NOUVEAU CR-P01 : `rebuild`/`rebuild_all` construisent le snapshot `media_group`/`media_group_member` via le même `aggregate_movies` que le live, appelé en fin de pipeline `main.py` ; writer-safe `run_with_retry` + session fraîche/tentative ; 1 pointeur membre par `(server_id, rating_key)`). **Feature « Télécharger » (Xtream)** : `download_service` (enqueue/confinement `resolve_confined`/transfert `download_to_disk`/préflight disque), `download_nfo` (`render_media_nfo`, sidecar `.nfo` best-effort post-complétion, lit `Media`). **Feature « Télécharger Plex » (C1→C7, NOUVEAU)** : `plex_api_service` (client plex.tv — resources/discovery + probe PMS candidate connections), `plex_sync_service` (`run_full_sync(session_factory) -> PlexSyncReport` : discover→probe→catalogue-sync mark-and-sweep→dédup, claim `plex_sync_status` idle→running + `already_running` + `reap_sync_status` boot master), `plex_catalog_service` (lectures pures secret-free : `list_unified`/`get_group`/`list_seasons_with_sources`/`list_episodes_with_sources`/`list_servers`/`get_sync_status`, dédup = `GROUP BY unification_id` SQL simple — pas de convergence mémoire, `unification_id` déjà résolu au sync), `plex_download_service` (`enqueue_plex_selection` scope `movie`/`series_all`/`seasons`/`episodes`, `resolve_job_urls` re-dérive au worker le couple `[url_download `?download=1``, url_stream direct-play sans `download=1`]`, jamais persisté ; `resolve_job_url` singulier = wrapper rétro-compat renvoyant le premier). **Fallback 403** : share avec « Allow Downloads » désactivé → `?download=1` renvoie 403 → `download_to_disk` bascule sur l'URL direct-play (octets identiques), voir §5.9.
- **`workers/`** — tâches : `sync_worker` (`run_all_accounts`/`sync_account` : VOD+séries+épisodes+Live+EPG, incrémental par `dto_hash`), `enrichment_worker` (`run`, TMDB, borné `ENRICHMENT_DAILY_LIMIT`), `health_check_worker` (`run` cron + `run_pipeline_validation` pipeline, HEAD→Range GET, circuit breaker), `embedding_worker` (`enqueue_rebuild`/`run_embedding_rebuild` re-embedding, jamais au boot), **`download_worker`** (feature « Télécharger » — `run_drain_loop`/`reap_orphans`/`_run_job`, **master-only**, concurrence `DOWNLOAD_CONCURRENCY` ; route sur `utils.server_id.is_plex_server_id(job.server_id)` pour dispatcher vers `plex_download_service.resolve_job_urls` (couple download+direct-play, **fallback 403 download-désactivé** via le param `fallback_urls` de `download_to_disk`) — Xtream et Plex **partagent ce seul worker**, aucune duplication).
- **`plex_generator/`** — `source` (`MediaSource` abstrait + **`DatabaseSource(account_ids=None)` agrégée multi-comptes** : charge tous les comptes actifs — ou un sous-ensemble — et **regroupe par `unification_id`** ; chaque groupe = 1 `PlexMovie`/`PlexSeries` portant N `versions`), `generator` (`PlexLibraryGenerator` → `SyncReport` created/updated/deleted/unchanged/**pruned**/errors/duration ; `_resolve_movie_names`/`_resolve_series_names` désambiguïsent les **groupes distincts** qui collisionnent sur `(titre, année)` via `_disambiguation_token` (token issu de l'id réel — `[tt…]`/`[tmdb…]`/`[<titre>]` — **jamais** `[imdb]`/`[tmdb]`/`[title_]`, qui collisionnaient sur disque) ; `_resolve_movie_names` applique **aussi** le tag `[XXX] ` aux films `is_adult` → **dossiers + `.strm` préfixés**), `storage` (`LocalStorage`/`DryRunStorage` + `_image_pool` ThreadPoolExecutor 8 threads, `storage.py:36`, écritures atomiques ; **`prune_orphan_dirs(live_dirs)`** balaie les dossiers de titre générés sans `.strm` vivant — comble la faille folder-aware quand les versions **changent** de dossier — en ne supprimant que des fichiers générés connus), `nfo_builder` (**préfixe `[XXX] ` le `<title>` du `movie.nfo` des films `is_adult`**), `naming` (chemins de versions ` - label` films **et** épisodes — convention **Plex + Jellyfin/Emby**, plus de tag `{edition-}` propriétaire Plex), `mapping` (`MappingStore` JSON, **clé version = `f"{server_id}:{rating_key}"`**), `models` (`PlexMovie` (+ **`is_adult`**)/`PlexSeries`/`PlexEpisode` + `PlexMovieVersion`/`PlexEpisodeVersion`, validator de rétro-compat single-version).
- **`db/`** — `database.py` (engine async SQLAlchemy, `async_session_factory`, `get_db` (`database.py:57-65`), `init_db` PRAGMA WAL + `register_sqlite_vec_listener` + `_VEC_LOADED` (`database.py:22-54,68-89`)), **`migrations.py`** (chaîne **001→019**, idempotentes, `run_migrations` `migrations.py:13-46` ; **015** = index `media` manquants (CR-P02), **016** = chiffrement mot de passe Xtream (CR-S03), **017** = tables snapshot `media_group`/`media_group_member` (CR-P01), **018** = tables `download_job`/`download_batch` (feature « Télécharger », Xtream), **019** = tables `plex_server`/`plex_media_item`/`plex_sync_status` (feature « Télécharger Plex », additives, isolées de `media`)).
- **`models/`** — `database.py` (entités SQLAlchemy : `Media` (+ **`is_adult`** + index `ix_media_adult` ; + **13 colonnes métadonnées NFO** `original_title`/`tagline`/`premiered`/`status`/`studio`/`country`/`tvdb_id`/`wikidata_id`/`imdb_rating`/`imdb_votes`/`tmdb_rating`/`tmdb_votes`/`cast_json` + index `ix_media_tvdb`, **M014**, peuplées par `nfo_import_service` uniquement), `XtreamAccount`, `XtreamCategory`, `LiveChannel` (+`is_adult` `database.py:173`), `EpgEntry`, `TvAuthSession`, `EnrichmentQueue` (+`existing_summary`), `TmdbScrapeCache`, **`AiSubtitleCache`** `database.py:255`, **`AiMediaBlurb`** `database.py:274`, **`ApiKey`** (table `api_keys`, clés par-utilisateur : revoke/expiry) `database.py:360`, **`MediaGroup`**/**`MediaGroupMember`** (snapshot unifié précalculé CR-P01, tables `media_group`/`media_group_member`, **M017**, écrites par `unified_group_service`), `DownloadJob`/`DownloadBatch` (feature « Télécharger » Xtream, **M018**, aucune colonne URL/credential), **`PlexServer`**/**`PlexMediaItem`**/**`PlexSyncStatus`** (feature « Télécharger Plex », **M019**, `database.py:529-647` ; `PlexServer.access_token` **chiffré Fernet** `EncryptedString`, `base_uri` sans token ; `PlexMediaItem` clé `(server_id, rating_key)`, isolée de `Media`, `unification_id` peuplé au sync)), `schemas.py` (Pydantic v2 ; **`MediaListResponse.next_cursor`** curseur keyset CR-P04 ; **`apply_adult_prefix`** + constante `ADULT_TITLE_PREFIX="[XXX] "` `schemas.py:13-18`, appliqué au `__init__` de `MediaResponse` `schemas.py:98` ; champ `is_adult` sur `MediaResponse`/`UnifiedMediaResponse`/`LiveChannelResponse` ; **`PlexServerResponse`/`PlexSourceResponse`/`PlexUnifiedItemResponse`/`PlexServerListResponse`/`PlexCatalogResponse`** camelCase, `schemas.py:585-646`, feature « Télécharger Plex »).
- **`utils/`** — `db_retry` (`commit_with_retry`/`run_with_retry`, retry « database is locked », `db_retry.py:24-26`), `metrics` (**5 métriques métier** + instrumentator, `metrics.py`), `payload_crypto` (Fernet, résolution clé `payload_crypto.py:34-47`), `request_context` (`request_id` contextvar + middleware), `string_normalizer`, `unification`, `ttl_cache`, `tasks` (`create_background_task` + jeu de réfs fortes), `time` (`now_ms`), `server_id` (`build_server_id`/`parse_server_id`).
- **`scripts/`** — `backup_db` (`sqlite3.Connection.backup` + pruning rétention), `strip_titles_pollution` (one-shot nettoyage titres). **`cli.py`** = Typer, **une seule** commande `generate` (`cli.py:68-69`).
- **`templates/admin/`** — templates Jinja2 de l'UI admin (`api/admin.py`).

> Règle d'archi : la logique métier vit dans `services/`/`workers/`, **pas** dans les routers (`api/` = validation + délégation). Les accès DB passent par `async_session_factory` / dépendance `get_db` (`db/database.py:57-65`).

## §3. Conventions
| Sujet | Règle | Référence |
|---|---|---|
| Langage / runtime | **CI : Python 3.13** ; **image Docker : Python 3.12-slim**. Async/await partout (FastAPI, SQLAlchemy[asyncio], httpx, aiosqlite) | `.github/workflows/tests.yml`, `Dockerfile:1` |
| Style / lint | **ruff + black + mypy + pytest-cov câblés** (CR-C06) : config `pyproject.toml` (`[tool.ruff]` select `E/F/W/I`, ignores ciblés → `ruff check` vert sans reformat de masse ; black en no-op documenté ; cov report-only) ; job lint CI + trigger `develop` (CR-T10) | `pyproject.toml`, `requirements-dev.txt`, `.github/workflows/tests.yml` |
| Validation | **Pydantic v2** aux frontières (alias camelCase via `to_camel`, `populate_by_name`, `response_model_by_alias=True`) ; jamais de dict brut en réponse publique (sauf dette `sync`/`categories`/`media`, cf. §10) | `api/ai.py:63`, `api/tv_auth.py:94`, `api/plex.py:16` |
| Erreurs | `HTTPException` aux frontières ; clé absente/invalide → **401 fail-closed**. Côté IA : **1 garde 503 au router `/api/ai`** (`verify_api_key` : sqlite-vec non chargé, `deps.py:76-80` ; ⚠️ l'ancien 503 « config absente » a disparu — `AI_API_KEY` vide donne **401**) + **1 motif 503 « AI model unavailable »** dans `ai.py` (fastembed KO → `EmbeddingUnavailableError` sur `/rank`/`/rank-multi`/`/search`/`/assistant`) + **1 motif 503 LLM distinct** `_ollama_503` (`/describe`/`/chat`/`/assistant`/`/blurb`/`/subtitles`) ; `/subtitles/translate` ajoute **422** (format) / **413** (taille). Cf. §9 | `api/deps.py:76-85`, `api/ai.py:351,450`, `api/ai.py:855`, `api/ai.py:1004,1009` |
| Auth | **FAIL-CLOSED sur toute l'API JSON** (confirmé empiriquement : `/api/media/movies` sans clé → 401). En-tête **`X-API-Key`** comparé en **temps constant** (`secrets.compare_digest`, `deps.py:46`) au secret maître **`AI_API_KEY`** **OU** à une clé par-utilisateur active (`api_keys`, digest SHA-256). `_guard=[Depends(verify_backend_secret)]` garde `accounts`/`categories`/`live`/`media`/`stream`/`sync`/`plex` (`main.py:399-405`) ; `/api/ai` → `verify_api_key` (module-level, `ai.py:55`) ; gestion des clés `/api/admin/keys` → `verify_master_key` (secret maître seul) ; `/admin`+`/docs`+`/openapi.json` → **Basic Auth** `verify_admin_basic_auth`. **Publics** : `/api/health` + `tv_auth` start/status/complete (`/approve` gardé). ⚠️ **0 test de rejet** sur ce garde (dette CR-T02, §10) | `api/deps.py:42-140`, `main.py:396-438`, `api/tv_auth.py:40,252` |
| Logs | logger `plexhub` (DEBUG fichier / INFO console, `main.py:62-66`) ; **`request_id`** injecté par filtre+middleware ; **jamais** de secret en clair (TMDB tronqué `config.py:108`) | `app/main.py`, `utils/request_context.py` |
| DB | SQLite **WAL** + **`busy_timeout=60000` (60 s)** + `connect_args timeout=60` sur chaque connexion du pool + cache 64 Mo + mmap 256 Mo (`database.py:15,86-91`), accès async (`aiosqlite`) ; écriture concurrente protégée par **`commit_with_retry`** (délais 0.1/0.5/1.0 s **au-dessus** du busy_timeout, `db_retry.py:21`). ⚠️ appliqué aux workers mais **pas** à tous les writers request-path (dette CR-C04) | `db/database.py`, `utils/db_retry.py` |
| Migrations | **idempotentes** (`CREATE … IF NOT EXISTS`, `ADD COLUMN` gardé par try/except) ; ajoutées **en fin de chaîne** dans `run_migrations()` (chaîne **001→017**) | `db/migrations.py:22-45` |
| Async I/O | tout appel bloquant (`sqlite3.backup`, init/inférence ONNX fastembed, téléchargement d'images = ThreadPool dédié) passe par `asyncio.to_thread` ; le LLM Ollama et la traduction de sous-titres sont I/O réseau **nativement async** (httpx). ⚠️ **exception connue** : `PlexLibraryGenerator.generate()` fait les écritures `.strm`/`.nfo` fsync + `mapping.save()` + `prune_orphan_dirs` (rglob/rmtree) **directement sur la boucle** (dette CR-C01 P1, §10) | `services/embedding_service.py:124,130`, `services/ollama_service.py:28-31`, `plex_generator/generator.py` |
| i18n métadonnées | `TMDB_LANGUAGE=fr-FR` par défaut, propagé à chaque requête TMDB ; LLM/blurb/assistant répondent **en français** par défaut (prompts FR) | `config.py:58`, `services/tmdb_service.py`, `api/ai.py:866,684` |
| Secrets | **jamais** dans le repo ; injectés via env / `.env` gitignored | `.env.example`, `config.py` |

## §4. Build / Run / Test
- **Lancer** : `uvicorn app.main:app --reload` (dev) ; conteneur via `docker-compose up` (**limite mémoire 2 Go**, `docker-compose.yml:39`, pour le modèle IA + ONNX).
- **CLI** : `python -m app.cli generate --output ./Media [--account-id … | --all] [--dry-run] [--strm-only]` (Typer).
- **Tests** : `pytest -v` (pytest-asyncio en **mode auto**, `testpaths=["tests"]`, `pyproject.toml`). CI `tests.yml` (Python 3.13, ubuntu) avec `--deselect` sur **un seul** test base64 (`tests.yml:33`). Run complet cette session : **484 passed, 1 deselected, 6 warnings** (~24 s). ⚠️ **le test désélectionné n'est PAS flaky** — il capture un vrai bug déterministe de décodage EPG base64 (`_try_base64_decode` corrompt « News/Test/Info/Kids » → dette CR-T01, §10). ⚠️ **CI ne se déclenche que sur push/PR vers `main`** (`tests.yml:5,7`) → la branche `develop` (où vit le delta v1.1.0→v1.1.5) **n'est pas gardée** (dette CR-T10). Aucun `pytest-cov`/lint câblé (CR-C06/T09).
- **Docker** : `docker.yml` (build/push GHCR sur push `main` + tags `v*`). `Dockerfile` (python:3.12-slim) + `docker-compose.yml` à la racine.
- **Versioning** : `APP_VERSION = "1.3.1"` dans `app/main.py:34` (release **v1.3.1**). Tag `vX.Y.Z` sur `main`. `GET /api/health` renvoie la **version live** (`health.py:33`, `request.app.version` = APP_VERSION) — plus de désalignement.

## §5. Flux clés (bout-en-bout)
- **§5.1 Sync** — `sync_worker.run_all_accounts()` (`sync_worker.py:1366`) → `sync_account(id)` (`:920`, lock `asyncio.Lock` par compte, skip non-bloquant si déjà en cours) → `xtream_service` (catégories, VOD, séries+épisodes, Live, EPG) → upsert incrémental par `dto_hash`/`content_hash` (INSERT..ON CONFLICT chunké, commits par 100/200) + `differential_cleanup*` (`:409/435/474/658`, movie/show/live). ⚠️ **les épisodes ne sont JAMAIS differential-cleaned** (orphelins permanents, CR-F01) et l'éviction par `page_offset` (`:557-576`) peut supprimer une ligne inchangée toujours listée sur un reorder provider (CR-F02) — §10. Planifié toutes `SYNC_INTERVAL_HOURS` (défaut 6 h).
- **§5.2 Enrichissement (scraping TMDB)** — au sync, les titres Xtream sont nettoyés par **`clean_title`** (préfixes langue/pays empilés, séparateurs scène `.`/`_`, année parenthésée OU nue, tags qualité partout, crochets). `enrichment_worker.run()` (Phase 1 movies, Phase 2 séries) résout chaque item :
  - **Cache de scrape persistant d'abord** (`scrape_cache_service` + table `tmdb_scrape_cache`, clé `media_type|titre_normalisé|année`) → **0 appel TMDB** pour un même film/série (cross-comptes + survit au redémarrage ; TTL 30 j match / 3 j négatif).
  - Sur miss : **matching façon ScraperMatcher** (`tmdb_service._best_match`) — `titleScore = max(sim(titre), sim(original_title))` (rapidfuzz `max(ratio, token_set_ratio)`), `yearScore` 1.0/0.8/0.5/**0.0** (mismatch = garde-fou homonyme), `confidence = 0.7·title + 0.3·year`, tri `confidence`>`vote_count`. **Auto-match** si `confidence≥0.85` ET `titleScore≥0.90` ET **marge≥0.05** vs 2ᵉ.
  - **Désambiguïsation par résumé** : si doute (marge < 0.05), on départage avec le **résumé Xtream** (`existing_summary`) vs `overview` candidat (`token_set_ratio`, seuils `SUMMARY_MIN_SIM=0.30` / `SUMMARY_TIEBREAK_MARGIN=0.10`).
  - **Fallback** : défaut → sans année → `language=en-US` → `/search/multi`. Le meilleur score est enregistré (`tmdb_match_confidence`) même sans match. Métrique `plexhub_tmdb_match_total{media_type,result}`.
  - Borné par `ENRICHMENT_DAILY_LIMIT` (défaut **50000**, appels API réels), concurrence 8, `MAX_ATTEMPTS=3`. Réf : `workers/enrichment_worker.py`, `services/tmdb_service.py`, `services/scrape_cache_service.py`, `utils/string_normalizer.py`.
- **§5.3 Validation de flux** — `health_check_worker.run_pipeline_validation()` (pipeline) et `run()` (cron) : HEAD puis Range GET (magic bytes), concurrence `STREAM_VALIDATION_CONCURRENCY` (défaut 20), marque cassé après `STREAM_BROKEN_THRESHOLD` (défaut 3) échecs ou échec **définitif** (404/403/error-CT/empty/magic-fail), re-check toutes `STREAM_VALIDATION_RECHECK_HOURS` (défaut 24), **circuit breaker** par compte à 90 % d'échecs. Réf : `workers/health_check_worker.py:274,89-103`.
- **§5.4 Dédup unifiée (DEUX consommateurs, logique partagée)** — la dédup par `unification_id` (imdb→tmdb→titre+année) vit dans **`aggregation_service`** et alimente **les deux** chemins. Le titre affiché du groupe est **nettoyé** (`canonical_title_year` : qualifieur VF/HD/VOSTFR + année embarquée strippés → « Terminator », 1984) ; le qualifieur reste sur le label de chaque version :
  - **(a) API REST (app PlexHubTV)** — endpoints `GET /api/media/{movies,shows}/unified` + `/episodes/unified?unification_id=…` : `media_service.get_unified_list`/`get_unified_episodes` agrègent **en mémoire** les lignes `media` (catégories autorisées) → 1 `UnifiedMediaResponse` par titre avec `versions[]` (serverId/ratingKey/label/isBroken). Les **épisodes agrégés** sont regroupés par `(saison, épisode)` à travers tous les comptes du groupe (membres = shows de même `unification_id`, épisodes matchés par `(server_id, grandparent_rating_key)`) → 1 `UnifiedEpisodeResponse` par créneau avec ses `versions[]`. Les endpoints bruts par-ligne (`/movies`, `/shows`, `/episodes`) restent inchangés (non-cassant). ⚠️ **P0 perf (CR-P01, §10)** : `get_unified_list` charge **TOUT** le catalogue autorisé, agrège et trie **sur la boucle d'événements**, puis découpe `limit/offset` **après** (`media_service.py:143-147`) → stall multi-seconde à 10⁴ lignes, pagination sans effet. ⚠️ le variant **`?unification_id=`** (`get_unified_group`, `media_service.py:149`) ne re-query que les lignes portant cette clé exacte → **sous-rapporte** les jumelles convergées (`imdb://`/`tmdb://`/`title_…`) que la liste, elle, fusionne (CR-F05).
  - **(b) Génération de bibliothèque NFO/.strm (Plex + Jellyfin/Emby)** — `_auto_generate_plex_library()` (si `PLEX_LIBRARY_DIR`) : **un seul arbre plat dédupliqué sur TOUS les comptes actifs** (plus de `output/{account_id}`). `DatabaseSource()` réutilise `aggregation_service` → `PlexLibraryGenerator` → `LocalStorage`. Chaque titre = **1 dossier + 1 `movie.nfo`/`tvshow.nfo` + 1 poster/fanart** ; chaque source/qualité/langue = un `.strm` distinct nommé ` - Label` (films **et** épisodes — convention **Plex + Jellyfin**, plus de tag `{edition-}` propriétaire). Épisodes agrégés par `(saison, épisode)` dans `Season XX/`, 1 `.nfo` par fichier-version. Suppression **folder-aware** : le NFO partagé survit tant qu'≥1 version vit dans le dossier. Exposé via `POST /api/plex/generate` (`accountId` optionnel restreint l'agrégation) et CLI (`python -m app.cli generate --all|--account-id`).
  - **Tagging adulte (+18 / XXX)** — à chaque sync, après le recalcul de visibilité, `category_service.update_media_adult_flags(db, account_id)` (`category_service.py:410`, appelé `sync_worker.py:1330`) reset puis flag `Media.is_adult=True` pour les **films** (`type='movie'`) dont la catégorie VOD est « adulte » (`_is_adult_category_name` matche `config.ADULT_CATEGORY_KEYWORDS`, OU `category_id` ∈ `config.ADULT_CATEGORY_IDS`) et force leur `content_rating=ADULT_CONTENT_RATING` (défaut `"XXX"`). Le préfixe `[XXX] ` (constante `ADULT_TITLE_PREFIX`, helper idempotent `apply_adult_prefix` `schemas.py:16`) est appliqué (a) à la **sérialisation API** (`MediaResponse`/`UnifiedMediaResponse`) **ET** (b) à la **génération de bibliothèque** : `generator._resolve_movie_names` le pose sur le nom résolu (→ **dossiers** + **fichiers `.strm`**, **après** `parse_title_year_and_suffix` qui sinon strippe le crochet de tête) et `nfo_builder.build_movie_nfo` sur le **`<title>` du `movie.nfo`** ; `PlexMovie.is_adult` porte le drapeau (`source.py` ⟵ `best.is_adult`). **Jamais persisté en DB** (`media.title` reste brut ; seul `content_rating="XXX"` est écrit → NFO `<mpaa>`). **Films `type='movie'` uniquement.** Champ `isAdult` sur `MediaResponse`/`UnifiedMediaResponse`. **Migration 013** (colonne `media.is_adult`).
  - **Convergence d'identité (anti-split `[imdb]`/`[tmdb]`/`[title_]`)** : la résolution per-ligne est instable (priorité imdb>tmdb dans `calculate_unification_id` ⇒ une ligne imdb+tmdb se clé `imdb://`, sa jumelle tmdb-seule `tmdb://`, et une non résolue `title_…` — **3 clés pour le même film**). `aggregation_service._converge` répare ça **côté lecture** (donc API `/unified` **ET** générateur) : Passe A fusionne par `imdb_id`/`tmdb_id` partagé (précis), Passe B absorbe la jumelle `title_…` de même titre+année (gardée contre titres dégénérés). **Ne modifie PAS `calculate_unification_id`** (contrat app Android). Effet mesuré sur la vraie base : ~1 279 groupes films re-fusionnés, 0 faux-merge.
  - **Aucune autre migration** : `unification_id` déjà peuplé au sync + recalculé à l'enrichissement. Réf : `services/aggregation_service.py`, `services/media_service.py`, `services/category_service.py:410`, `api/media.py`, `plex_generator/source.py`, `app/main.py:77-118`.
- **Pipeline planifié (master seul)** : sync → enrichissement → validation → génération Plex, en série (`scheduled_sync_enrich_generate`, `main.py:248-273`), `interval=SYNC_INTERVAL_HOURS`, `max_instances=1`, `coalesce=True`, `misfire_grace_time=300`. Un run **initial non-bloquant** au boot du master (`main.py:311-321`). ⚠️ Le run initial et le run d'intervalle **ne partagent aucune exclusion mutuelle** (`max_instances=1` ne sérialise que l'intervalle) → recouvrement possible sur un 1ᵉʳ sync lent (cf. §10). + crons (master) : health-check `run()` (`hour=2`), cleanup EPG périmé (`hour=3`), backup DB (`hour=BACKUP_HOUR`, défaut 4) si `BACKUP_ENABLED`. Réf : `main.py:274-307`.
- **§5.5 Recommandations IA (vectorielles)** — `POST /api/ai/rank` (item→item, `ai.py:320`) / `POST /api/ai/rank-multi` (centroïde pondéré décroissant 1.0,0.9,… min 0.1, `ai.py:467`) → résolution refs (imdb→tmdb via `find_by_imdb_id`) → `load_cached_vectors` → `hydrate_misses` (cap 20, timeout 10 s/tâche, `recommendation_service.py:31-32`) → `cosine_rank`. `embedding_service` = **fastembed** `sentence-transformers/paraphrase-multilingual-MiniLM-L12-v2` (384 dim, `embedding_service.py:26-27`) + **sqlite-vec** (`vec0 FLOAT[384]`), cache `ai_tmdb_cache`. **Explications « why recommended » (F5)** : si `explain=true` dans le body, les **5 premiers** résultats (`EXPLAIN_CAP`, `ai.py:191`) reçoivent une phrase FR générée par gemma4 (`_explain_items` `ai.py:220`, concurrence 4, timeout 20 s/tâche, **best-effort** → `explanation=null` si Ollama KO, **jamais de 503**). Diagnostics : `GET /embed/status` (`ai.py:748`), rebuild async `POST /embed/rebuild` (**202** + `jobId`, `ai.py:716`), `GET /embed/jobs/{id}` (`ai.py:732`). Réf : `api/ai.py`, `services/recommendation_service.py`.
- **§5.6 Appairage TV** — `tv_auth` device-flow (RFC 8628-like) : `POST /api/tv-auth/start` (TV non authentifiée → `deviceCode`+`userCode`, 201) → `POST /api/tv-auth/approve` (**authentifié X-API-Key**, attache payload chiffré) → `GET /api/tv-auth/status` (TV poll, payload **livré une seule fois**) → `POST /api/tv-auth/complete` (one-shot, scrub). Payload chiffré **Fernet** (`payload_crypto`), TTL `TV_AUTH_TTL_SECONDS` (défaut 900 s, `config.py:30`). ⚠️ Incohérence de contrat (CR-F06, §10) : `GET /status` attend un paramètre **snake_case** `device_code` (`tv_auth.py:298`) alors que le reste de l'API est camelCase (`deviceCode`) ; **+ livraison one-shot non atomique** (CR-F07, race sur double poll `tv_auth.py:314-331`). Réf : `api/tv_auth.py`.
- **§5.7 Génération LLM (Ollama / gemma4)** — le router `/api/ai` (donc **authentifié X-API-Key — 401 fail-closed sinon**, et soumis au garde 503 sqlite-vec de `deps.py:76-80`) expose 3 endpoints qui délèguent à `services/ollama_service.py` (httpx async vers `OLLAMA_URL`/`OLLAMA_MODEL`) :
  - **`POST /api/ai/describe`** (`ai.py:863`) : construit un prompt FR/EN « expert recommandations » à partir de `{title, overview, genres, year, language}` → `ollama_service.generate()` (`/api/generate`, `stream=False`) → `{recommendation, model}`.
  - **`POST /api/ai/chat`** (`ai.py:886`) : chat libre. `stream=False` → `ollama_service.chat()` (`/api/chat`) → `{reply, model}` ; `stream=True` → `StreamingResponse` **SSE** (`text/event-stream`) via `ollama_service.stream_generate()` du **dernier message** (`/api/generate` ligne-par-ligne), terminé par `data: [DONE]` (ou `data: [ERROR]` sur exception, sans propager de 503 — le flux est déjà ouvert).
  - **`GET /api/ai/llm/status`** (`ai.py:912`) : `ollama_service.is_healthy()` interroge `GET /api/tags` (timeout 5 s) et vérifie que `OLLAMA_MODEL` figure dans les modèles installés → `{healthy, model, ollamaUrl, detail}`.
  - **503 LLM distinct** : toute exception sur `/describe` et `/chat` (non-stream) → `_ollama_503` (`ai.py:855-860`), message `"LLM unavailable — Ollama unreachable or model not loaded"` — **différent** du 503 « AI model unavailable » (fastembed). Réf : `api/ai.py:793-921`, `services/ollama_service.py`, `config.py:60-62`.
- **§5.8 Pack IA « feature pack » (search / assistant / blurb / sous-titres)** — tous sur le router `/api/ai` (**authentifié — 401 fail-closed — + soumis au garde 503 sqlite-vec `deps.py:76-80`**), commit `8759a51` :
  - **`POST /api/ai/search`** (recherche sémantique NL, `ai.py:499`) : (1) **reformulation best-effort** de la requête via gemma4 (timeout 15 s ; si Ollama KO → requête brute, **PAS de 503**), (2) `embed_query` fastembed (KO → **503 « AI model unavailable »**), (3) `semantic_search` = KNN `vec0 MATCH … AND k=:k` joint à `ai_tmdb_cache`, filtre `media_type` **post-KNN** (over-fetch `min(limit*4,200)` puis tronque — vec0 n'a pas de colonne type, `recommendation_service.py:211`), `cosine = 1 - dist²/2` (vecteurs L2). → `{results[], queryUsed, model}`.
  - **`POST /api/ai/assistant`** (assistant catalogue RAG, `ai.py:623`) : (1) `embed_query` du message (KO → 503), (2) `semantic_search_with_overview` (top-`limit`, 1≤limit≤10), (3) prompt système FR « réponds UNIQUEMENT avec les titres du catalogue fourni » + bloc contexte (titre + overview tronqué 300 c) + `history[]` + message → `ollama_service.chat()`. Échec Ollama → **`_ollama_503`**. → `{reply, sources[], model}` (`sources` = titres de grounding).
  - **`POST /api/ai/blurb`** (`ai.py:1084`) + **`GET /api/ai/blurb/{tmdb_id}?mediaType=&lang=`** (`ai.py:1201`) : synopsis FR court + 3-6 mood tags via `generate_blurb` (gemma4, parse JSON robuste + retry strict + fallback). Cache `ai_media_blurb` clé `(tmdb_id, media_type, lang)` (**M012**) ; `force=true` bypasse le cache ; source texte = body sinon `ai_tmdb_cache`. Miss sans source → **422**. Échec Ollama → **`_ollama_503`**. GET inexistant → **404**. → `{tmdbId, mediaType, lang, summary, tags[], cached, model}`.
  - **`POST /api/ai/subtitles/translate`** (`ai.py:956`) : traduit un fichier **SRT ou VTT** vers `targetLang` (défaut `fr`) via `subtitle_service.translate_subtitles` (parse → chunks de `SUBTITLE_CHUNK_CUES` cues traduits en parallèle `SUBTITLE_CONCURRENCY`, re-sérialise, timeout global `SUBTITLE_TOTAL_TIMEOUT`). Cache `ai_subtitle_cache` clé `SHA-256(content|targetLang|format|sourceLang|model)` (**M011**, hit → `cached=true`, `durationSeconds=0`). Erreurs : format invalide → **422** (`SubtitleFormatError`), trop gros (`SUBTITLE_MAX_BYTES`/`SUBTITLE_MAX_CUES`) → **413** (`SubtitleTooLargeError`), panne LLM/transport → **`_ollama_503`**. → `{translatedContent, format, cueCount, cached, model, durationSeconds}`. Cleanup cron quotidien (`SUBTITLE_CACHE_RETENTION_DAYS`, défaut 30). Réf : `api/ai.py`, `services/subtitle_service.py`, `services/recommendation_service.py`.
- **§5.9 Téléchargement Plex (« Télécharger Plex », C1→C7)** — bout-en-bout : **discovery** (`plex_api_service` interroge plex.tv/api/resources avec `PLEX_ACCOUNT_TOKEN` → serveurs possédés + partagés) → **probe** (chaque connexion candidate testée sous `PLEX_PROBE_TIMEOUT`, la première joignable gagne → `plex_server.base_uri`/`is_reachable`) → **catalogue-sync** (`plex_sync_service.run_full_sync`, mark-and-sweep par `synced_at` sur `plex_media_item`, un serveur à la fois, claim `plex_sync_status` idle→running pour empêcher tout chevauchement — manuel via `POST /admin/plex-downloads/sync` OU cron optionnel `plex_catalogue_sync` si `PLEX_SYNC_INTERVAL_HOURS>0`, gardé `if is_master:`, ajouté `main.py` avant `scheduler.start()`) → **dédup** (`unification_id` calculé/rafraîchi au sync, priorité imdb>tmdb>`plexsrc://`, jamais titre+année seul ; lecture = simple `GROUP BY unification_id` SQL dans `plex_catalog_service.list_unified`/`get_group`, pas de convergence mémoire façon `aggregation_service`) → **enqueue** (`POST /admin/plex-downloads` → `plex_download_service.enqueue_plex_selection`, scope `movie`/`series_all`/`seasons`/`episodes`, écrit dans la table `download_job` **partagée** avec la feature Xtream, `server_id` préfixé `plex_<clientIdentifier>`) → **worker** (le **même** `download_worker` que Xtream, routé par `is_plex_server_id(job.server_id)` vers `plex_download_service.resolve_job_urls` — pluriel — pour re-dériver au dernier moment un couple d'URLs `[url_download, url_stream]` jamais persistées : `[0]` = `?download=1` (fichier original), `[1]` = **URL direct-play** du même `Part.key` **sans `download=1`**. **Fallback download-désactivé (`/refacto`)** : si le propriétaire du partage a désactivé « Allow Downloads », l'URL `?download=1` renvoie **403** ; `download_service.download_to_disk` (nouveau param `fallback_urls`) **bascule alors sur l'URL direct-play** — octets **identiques** au fichier original, Range-capable, gardée par la permission *lecture* seule — **sans consommer le budget de retry**. `fallback_urls=[]` côté Xtream ⇒ 403 reste permanent (chemin inchangé). Escalade P3 si un serveur bloque *aussi* le direct-play (transcode-only) : capture HLS + remux ffmpeg, non livré). Lecture JSON (QA/automation, ticket **C7**) : `GET /api/admin/plex-downloads/{servers,catalog,catalog/{type}/{unification_id}}` (`app/api/plex_downloads.py`, garde `verify_master_key`, jamais `access_token`/`base_uri` dans la réponse). Réf : `services/plex_api_service.py`, `services/plex_sync_service.py`, `services/plex_catalog_service.py`, `services/plex_download_service.py`, `api/admin_plex_downloads.py`, `api/plex_downloads.py`, `workers/download_worker.py` (branchement `is_plex_server_id`), `docs/10-prd-plex-download.md`.

## §7. Agents & ownership (`.claude/agents/` — 21 fichiers)
**Direction/orchestration** : `ceo`, `cpo`, `cto`, `tech-lead`, `tech-manager`. **IC** : `backend-developer`. **Qualité/gate** : `qa-engineer`, `code-reviewer`, `security-reviewer`, `integration-agent`. **Run/ops** : `devops-engineer`, `release-manager`, `perf-benchmarker`, `observability-analyst`. **Audit/contexte** : `cleanroom-auditor`, `cleanroom-fixer`, `a0-cartographer`. **Spécialistes domaine** : `db-migration-specialist` (schéma SQLite/migrations), `sync-specialist` (Xtream/sync/enrichment), `ai-recsys-specialist` (embeddings/ranking/sqlite-vec), `plex-generator-specialist` (génération NFO/arbo).

## §7bis. Skills par agent
`house-conventions` (charge les packs `.claude/knowledge/`) pour tous ; `parallel-orchestrator` (tech-manager) ; `architecture-builder` (cto/tech-lead) ; `requirements-intake`/`prd-builder` (cpo) ; `sprint-planner` (tech-manager) ; `brownfield-onboarding` (a0-cartographer). Skills marketplace utiles : `systematic-debugging` (incident), `code-review` (reviewers), `security-audit` (security-reviewer), `production-code-audit` (cleanroom/cartographer), `api-design-principles` (cto/backend-developer).

## §9. Pièges (house law — à respecter impérativement)
1. **Cold start IA ~30 s** au 1ᵉʳ `/rank` : fastembed télécharge + charge les poids ONNX (cold start documenté `embedding_service.py:11`). Les appels suivants sont rapides (singleton `_model`, lock `_MODEL_LOCK`, `embedding_service.py:71-82`). Override via `AI_EMBED_MODEL` ; cache persistant via `AI_EMBED_CACHE_DIR`.
2. **Motifs 503 IA** (contractuels) : **1 garde au niveau du router `/api/ai`** (`verify_api_key`, `deps.py:76-80`) — `AI vector storage unavailable` (sqlite-vec non chargé). ⚠️ **CHANGÉ** : l'ancien 503 `AI service not configured` (AI_API_KEY vide) **n'existe plus** — une clé absente/vide donne désormais **401 fail-closed** (`_authenticate`→False, `deps.py:64-68`), pas 503. **+ 1 motif endpoint** `AI model unavailable` (fastembed KO → `EmbeddingUnavailableError`, sur `/rank` `ai.py:351`, `/rank-multi` `ai.py:450`, `/search`, `/assistant`). **Le motif 503 LLM est distinct** (`_ollama_503`, cf. piège 12). Le garde sqlite-vec s'applique à **TOUS** les endpoints `/api/ai`, y compris LLM pur (cf. piège 12).
3. **Épisodes/personnes non supportés par l'IA vectorielle** : ranking au niveau `movie`/`tv` uniquement (`Literal["movie","tv"]`, `ai.py:87,104`). `find_by_imdb_id` ignore défensivement `tv_episode_results`/`tv_season_results`/`person_results` (`tmdb_service.py:281`) → comptés en `resolutionFailed` et droppés.
4. **Cap 20 fetches TMDB frais par `/rank`** : `HYDRATE_CAP = 20`, timeout 10 s/tâche (`recommendation_service.py:31-32`) ; le surplus + timeouts + échecs → `cacheMissesDropped` (`stats.dropped`). Le client re-appelle une fois le cache chaud.
5. **Le rebuild ne tourne JAMAIS au boot** : uniquement via `POST /api/ai/embed/rebuild` (`embedding_worker.py:3,132`). Idempotent : scan `embedded_at IS NULL`, curseur `tmdb_id > :cursor` (`embedding_worker.py:76`), `PAGE_SIZE=50`, DELETE-puis-INSERT sur `vec0` (UPSERT interdit sur table virtuelle, `embedding_worker.py:108-114`). Jobs en mémoire, `JOBS_CAP=100`.
6. **Migrations idempotentes** : tout DDL en `IF NOT EXISTS` / `ADD COLUMN` gardé ; nouvelle migration **en fin** de `run_migrations()`. **Ordre d'exécution : 001-007, puis 008 (sur connexion dédiée `migrations.py:34-35`), puis 009→017** (`migrations.py:22-45`). M008 (`ai_embeddings` vec0 `FLOAT[384]` + `ai_tmdb_cache`, `migrations.py:414-448`) **dépend du chargement de sqlite-vec** (`register_sqlite_vec_listener`, `database.py:25-54`). Migration courante = **017**. Chaîne réelle : 010 `tmdb_scrape_cache`+`existing_summary`, 011 `ai_subtitle_cache`, 012 `ai_media_blurb`, 013 `media.is_adult`, 014 `media` métadonnées NFO (`original_title`/`tagline`/`premiered`/`status`/`studio`/`country`/`tvdb_id`/`wikidata_id`/`imdb_rating`/`imdb_votes`/`tmdb_rating`/`tmdb_votes`/`cast_json`), **015** index `media` manquants (CR-P02, `CREATE INDEX IF NOT EXISTS` alignés sur `__table_args__`), **016** chiffrement des mots de passe Xtream au repos (CR-S03, `EncryptedString` Fernet), **017** tables snapshot `media_group`/`media_group_member` (CR-P01, `CREATE TABLE/INDEX IF NOT EXISTS`, additives).
7. **`fcntl.flock` = POSIX** : l'élection master-worker (`main.py:226-227`) ne fonctionne que sous Linux/Docker (pas Windows natif). Seul le master lance le scheduler ; fermer le fd libère le flock.
8. **SQLite « database is locked »** : passer par `commit_with_retry` / `run_with_retry` (`db_retry.py:21`, délais 0.1/0.5/1.0 s au-dessus du **`busy_timeout=60000` (60 s)** + `connect_args timeout=60`, `database.py:15,90`). WAL activé. Ne jamais ouvrir plusieurs writers sans retry. ⚠️ les writers request-path (`tv_auth`/`accounts`/`categories`/services) utilisent encore `db.commit()` nu, pas `commit_with_retry` (dette CR-C04).
9. **Rotation de logs Windows** : `SafeRotatingFileHandler` avale les `PermissionError` (`main.py:30-35`) quand un autre process tient le fichier. Ne pas supprimer ce garde-fou.
10. **CORS** : `CORS_ORIGINS` **défaut `*`** (`config.py:61-62`) + méthodes/headers `*` (`main.py:385-386`) — **expliciter en prod** (dette CR-S06 ; atténué car auth par en-tête `X-API-Key` + `allow_credentials=False`). Tokens/clés jamais loggés ni renvoyés. ✅ **Rappel** : toute l'API JSON est désormais **fail-closed** (`verify_backend_secret`) — l'ancien « seuls /api/ai + /approve authentifiés » est CADUC. ⚠️ Reste ouvert : **`/metrics` non authentifié** sur le tunnel public (dette CR-S02, §10).
11. **Appels bloquants** (`sqlite3.backup`, init/inférence ONNX `embedding_service.py:124,130`) → `asyncio.to_thread` ; ne jamais bloquer la boucle d'événements. Les téléchargements d'images Plex passent par un `ThreadPoolExecutor` dédié (8 threads, `storage.py:36`).
12. **503 LLM Ollama distinct du 503 fastembed** : sur `/describe`/`/chat` (non-stream)/`/assistant`/`/blurb`/`/subtitles`, toute exception Ollama → `_ollama_503` (`ai.py:855`), message `"LLM unavailable — Ollama unreachable or model not loaded"`. NB **couplage à connaître** : ces endpoints sont **aussi** soumis au garde sqlite-vec du router (dépendance module `verify_api_key`, `ai.py:55`) : si sqlite-vec n'est pas chargé, **même les endpoints LLM purs** (`/describe`/`/chat`/`/llm/status`/`/subtitles`) renvoient **503** (`AI vector storage unavailable`) **avant** d'atteindre Ollama, alors qu'ils n'utilisent ni embeddings ni vec0 ; et une clé absente/invalide donne **401** avant tout.
13. **Dépendance réseau + cold start Ollama** : `ollama_service` parle à `OLLAMA_URL` (défaut `http://khoj-ollama:11434`, hôte Docker — **injoignable en dev local hors compose**). Timeouts httpx : génération **120 s**, status **5 s** (`ollama_service.py:21-22`). Premier appel = chargement du modèle `gemma4:e4b` en VRAM/RAM côté Ollama (latence). Aucun client SDK : chaque appel ouvre un `AsyncClient` éphémère (pas de pool partagé).
14. **SSE chat = pas de 503 propre** : en mode `stream=True`, le flux est déjà ouvert ; une panne Ollama émet `data: [ERROR]\n\n` puis ferme (`ai.py:899-901`), jamais un code HTTP d'erreur. Idem la **reformulation `/search`** et les **explications `/rank`** dégradent silencieusement (jamais de 503). Le client doit gérer ces sentinels.
15. **Tagging adulte = films seulement ; préfixe appliqué aux frontières de sortie, jamais persisté** : `update_media_adult_flags` ne traite que `type='movie'` (`category_service.py:445`) ; le préfixe `[XXX] ` est appliqué à la **sérialisation API** (`schemas.py:98`) **ET à la bibliothèque générée** (dossiers + `.strm` via `generator._resolve_movie_names` ; `<title>` du NFO via `nfo_builder.build_movie_nfo` ; drapeau `PlexMovie.is_adult`), mais **jamais persisté** → ne jamais lire `media.title` pour déduire l'état adulte (lire `is_adult`). ⚠️ Le préfixe est posé **après** `parse_title_year_and_suffix` (qui strippe un crochet `[...]` de tête `string_normalizer.py:6`) — **ne pas** le préfixer en amont dans `source.py` (il serait perdu pour les chemins). Le `content_rating="XXX"` est en revanche **écrit en DB** (NFO `<mpaa>`). Reset complet à chaque sync (idempotent/rétroactif).
16. **Sous-titres : limites dures** : `/subtitles/translate` rejette `>SUBTITLE_MAX_BYTES` (défaut 2 Mo) ou `>SUBTITLE_MAX_CUES` (défaut 3000) en **413** ; format non SRT/VTT en **422**. Sentinelle de saut de ligne intra-cue ` ⏎ ` (`subtitle_service.py:48`) supposée absente du texte source ; sur mismatch de comptage le chunk garde le texte d'origine (aucune cue perdue/décalée).
17. **Feature « Télécharger » (téléchargement physique)** : (a) **worker master-only** — `download_worker.run_drain_loop` ne tourne que sous `if is_master:` (`main.py`, `fcntl` POSIX, piège 7) ; l'enqueue (écriture de lignes `download_job`) marche sur tout worker, mais seul le master draine. (b) **`DOWNLOAD_DIR` vide ⇒ feature no-op** (garde locale au démarrage du worker + service) : rien ne se télécharge tant que le dossier dédié n'est pas configuré dans `.env` (**distinct de `PLEX_LIBRARY_DIR`**). (c) **URL Xtream = secret** : elle contient user/pass, elle est re-dérivée au worker (`stream_service.build_stream_url`) et ne doit **JAMAIS** être persistée en DB, loggée (même `exc_info=True`), ni renvoyée en API/HTML — ne pas régresser. (d) **Confinement d'écriture (F-007)** : toute destination passe par `download_service.resolve_confined` (realpath sous `DOWNLOAD_DIR`) ; **aucun chemin client** n'est accepté — ne pas introduire de `outputDir`-like (dette CR-S01). (e) `download_to_disk` = **`follow_redirects=False`** (anti-SSRF, DL-01) : ne pas réactiver le suivi de redirection. (f) `.part` + `os.replace` atomique + reprise `Range` : ne jamais promouvoir un `.part` sur cancel/échec. Migration **018** (tables `download_job`/`download_batch`).

## §10. État réel (re-cartographié 2026-07-11, HEAD code `1879f83`, branche `develop`)
**Stack runtime** (`requirements.txt`) : **`fastapi>=0.115,<0.116`** ⚠️ ÉPINGLÉE · uvicorn[standard]≥0.27 · sqlalchemy[asyncio]≥2.0 · aiosqlite≥0.20 · httpx≥0.27 (**aussi le client Ollama — aucun SDK dédié**) · pydantic≥2.6 · pydantic-settings≥2.1 (déclarée, **non utilisée** par `config.py`) · apscheduler≥3.10 · rapidfuzz≥3.6 · python-dotenv · typer≥0.9.0 · **`prometheus-fastapi-instrumentator>=7.0,<8`** ⚠️ ÉPINGLÉE · prometheus-client≥0.20 · jinja2≥3.1 · python-multipart≥0.0.9 · **fastembed≥0.7,<1.0** · onnxruntime≥1.20,<2.0 · **sqlite-vec≥0.1,<0.2** · numpy≥1.26,<3.0 · psutil≥6.0,<7.0 · cryptography≥42,<46. **Le pack IA n'ajoute AUCUNE dépendance pip** (subtitles + LLM réutilisent httpx ; search/assistant/blurb réutilisent fastembed + sqlite-vec). ⚠️ **Les bornes FastAPI/instrumentator sont liées** (FastAPI 0.116+ casse le route-walker de l'instrumentator 8.x → tous les `/api/*` en 500 ; outage `a8a0ce7`) — ne pas les délier.
**Stack tests** (`requirements-dev.txt`) : pytest≥7.0 · pytest-asyncio≥0.23 · respx≥0.21.
**Runtimes** : CI = Python **3.13** (`tests.yml:20`) ; image Docker = **python:3.12-slim** (`Dockerfile:1`) ; `docker-compose.yml` limite mémoire **2 G** (`docker-compose.yml:39`).
**LLM** : Ollama via env `OLLAMA_URL`/`OLLAMA_MODEL` (`config.py:60-62`) ; **aucune dépendance pip** (httpx réutilisé).
**Migration courante** : **020** (colonne `media.file_size`, taille de fichier peuplée au health-check) ; chaîne complète 001→020 (`db/migrations.py` ; 015 = index `media` manquants CR-P02, 016 = chiffrement Xtream CR-S03, 017 snapshot `media_group`, 018 `download_job`/`download_batch`, 019 tables Plex `plex_server`/`plex_media_item`/`plex_sync_status`, 020 `media.file_size`).
**Métriques Prometheus métier** (`utils/metrics.py`, 5 métriques) : `plexhub_sync_duration_seconds` (Histogram), `plexhub_tmdb_requests_total` (Counter, `kind`×`result`), **`plexhub_tmdb_match_total`** (Counter, `media_type`×`result=matched|nomatch|ambiguous`, `metrics.py:27-31`), `plexhub_streams_alive_ratio` (Gauge), `plexhub_enrichment_queue_size` (Gauge) + métriques HTTP auto via instrumentator sur `/metrics`.
**Résolu depuis le dernier refresh (ne plus traiter comme dette)** : ① **auth incomplète / catalogue-sync-plex OUVERTS** (ancien P0) → **fail-closed** sur toute l'API JSON (`main.py:399-405`, confirmé empiriquement) ; ② **version `/api/health` codée en dur** → version live (`health.py:33`) ; ③ **CI factice / rouge** → 484 passed, 1 deselected.

**Dette ouverte** (audit clean-room indépendant `docs/audit/cleanroom-2026-07-11/` — **56 findings : 1 P0, 17 P1, 26 P2, 12 debt** ; nouveau schéma d'IDs `CR-*` qui **supersede** l'ancien `cleanroom-2026-06-15`. Chaque item recoupé au code) :
- **CR-P01 (P0) — RÉSOLU (2026-07-12, `/refacto` étape 3, `c3024e3`)** : le browse **non-filtré** `/movies|shows/unified` pagine désormais côté SQL (`LIMIT`) sur la table dénormalisée **`media_group`** (+`media_group_member`, migration 017) construite en fin de pipeline par `services/unified_group_service.rebuild_all`, puis ré-hydrate/ré-agrège **la page seule** (`media_service._unified_list_from_snapshot`) → O(page), cartes/`versions[]` octet-identiques. **Fallback live** si snapshot vide (DB fraîche) ou requête **filtrée** (search/genre/year — le filtre change la composition des groupes). Le cache TTL de la Vague B reste le chemin des requêtes filtrées.
- **CR-S01 (P1) — écriture FS arbitraire post-auth** : `POST /api/plex/generate` prend `outputDir` client verbatim → `Path()` → `LocalStorage` **sans confinement** (`plex.py:39,45,56` → `storage.py`). Toute clé **par-utilisateur** (pas seulement le maître) peut écrire des fichiers n'importe où + **exfiltrer les creds Xtream** des `.strm` d'autres comptes. (≠ l'ancien CR-S01 « auth incomplète », lui RÉSOLU.)
- **CR-F02 (P1) — éviction par slot supprime des lignes vivantes** : Phase-1 de `upsert_media_batch` évince par `(server_id, …, page_offset)` (`sync_worker.py:557-576`) ; sur un reorder provider, une ligne inchangée toujours listée est supprimée puis re-INSÉRÉE fraîche (perte de `tmdb_id`/`unification_id`).
- **CR-T02 (P1, borderline P0) — 0 test de rejet auth** : `grep verify_backend_secret tests/` = 0. Le garde fail-closed de toute l'API catalogue/sync/plex tient sur `dependencies=_guard` (`main.py:396-405`) sans aucun test 401 → un drop du garde **passe vert**.
- **CR-T01 (P1) — bug EPG base64 masqué par un test « flaky »** : `_try_base64_decode` (`live.py`, utilisé `:221,224`) rejette les chars `<32` mais **pas** `U+FFFD` → corrompt « News/Test/Info/Kids » (titres EPG courants). Le test correct est **désélectionné en CI** (`tests.yml:33`) comme « flaky » alors qu'il échoue **déterministe**.
- **CR-F04 (P1) — pipeline boot vs intervalle sans mutex** : `initial_sync_then_enrich` (`main.py:328-338`, hors scheduler) et `scheduled_sync_enrich_generate` (`:249-274`, `max_instances=1` ne sérialise que l'intervalle) → sur 1ᵉʳ boot lent, enrichment + génération concurrents = **double budget TMDB** + arbre/`.plex_mapping.json` racy.
- **CR-P02 (P1) — index ORM `media` absents des DB upgradées** : ~15 index composés déclarés dans `models/database.py:105-124` ne sont créés que par `create_all` sur table **fraîche** ; les migrations n'en ré-assurent que ~4 → full-scans + filesorts sur les listes chaudes en prod.
- **CR-F03 (P1) — `ENRICHMENT_DAILY_LIMIT` sous-compté** : `api_used` compte des items/appels logiques, pas les ≤4 retries HTTP de `tmdb_service._request`, et reset chaque run → dépassement **2-4×** sous rate-limit.
- **CR-F01 (P1) — épisodes jamais differential-cleaned** : `differential_cleanup*` couvre movie/show/live seulement (`sync_worker.py`) → épisodes orphelins permanents (série délistée ou renumérotée).
- **CR-C01 (P1) — génération sur la boucle d'événements** : `PlexLibraryGenerator.generate()` fait écritures `.strm`/`.nfo` fsync + `mapping.save()` + `prune_orphan_dirs` (rglob/rmtree) **non offloadés** (`generator.py`) → boucle affamée au boot et à chaque pipeline (seules les images passent en ThreadPool). Même racine que CR-P01.
- **CR-P03 (P1) — recherche = double full-scan** : `ILIKE '%term%'` non-sargable + COUNT-over-subquery `SELECT *` (`media_service.py:52,55,77-79` ; `live.py:63,66-67`) → 2 scans complets par requête de recherche.
- **CR-A01/A02/A03 (P1) — archi** : logique métier + SQL brut dans routers `live`/`accounts`/`categories`/`stream` ; orchestration Plex-gen **triplée** (`main.py`/`plex.py`/`cli.py`) + `sync.py:74` importe le symbole privé `app.main._auto_generate_plex_library` ; god-files `sync_worker.py` (1390, `sync_account` = 446 lignes), `ai.py` (1228), `nfo_import_service.py` (888).
- **CR-F05 (P1) — `/unified?unification_id=` sous-rapporte** : `get_unified_group` (`media_service.py:167-179`) ne re-query que la clé exacte → drop les jumelles convergées que la liste fusionne.
- **CR-C02 (P1) — fuite snake_case** : `refresh_categories` renvoie un dict brut `vod_count`/`series_count` (`categories.py:145-150`) sur une API sinon 100 % camelCase.
- **P2 contrats/data-flows** : **CR-C03** dicts non typés sans `response_model` (`sync.py` 7/8 endpoints, `media.py:353`, `categories.py`) → absents d'OpenAPI ; **CR-F06** tv-auth `?device_code` snake_case (`tv_auth.py:298`) vs camelCase ailleurs ; **CR-F07** livraison one-shot tv-auth non atomique (`tv_auth.py:314-331`) ; **CR-F08** circuit breaker validation évalué une seule fois à exactement 50 checks (`health_check_worker.py:459-462`) ; **CR-F09** convergence Passe B non déterministe (ordre de requête, `aggregation_service.py:186-198`) ; **CR-F11** épisodes d'une série DTO-inchangée jamais re-sync (`sync_worker.py:1148-1152`).
- **P2 sécurité (hardening)** : **CR-S02** `/metrics` non authentifié sur le tunnel public (`metrics.py:46-51`, `main.py:442`, expose `account_id`) ; **CR-S03** mots de passe Xtream **en clair** au repos + backups non chiffrés (`database.py:136`) ; **CR-S04** clé Fernet tv-auth **dérivée du secret bearer** `AI_API_KEY` par défaut (réutilisation de clé, `payload_crypto.py:34-47`) ; **CR-S05** aucun rate-limit / anti-brute-force ; **CR-S06** CORS `*` (`config.py:61-62`, `main.py:385-386`) ; **CR-S07** CSRF sur `/admin` (Basic Auth + form POST, pas de token) ; **CR-S08** SSRF post-auth via `base_url` compte + URLs d'images (`follow_redirects=True`).
- **P2 perf** : **CR-P04 RÉSOLU (2026-07-12, `b1f5ed6`)** curseur keyset **optionnel/additif** sur les listes brutes (tri récence, offset conservé, `next_cursor`) ; **CR-P05** générateur + validation pipeline matérialisent tout le catalogue en RAM (health_check partiellement résolu) ; **CR-P06** `ORDER BY random()` full-scan pour l'échantillon health-check (`health_check_worker.py:263`) ; **CR-P07 RÉSOLU (2026-07-12, `ba6689e`)** sérialisation single-pass (`_single_pass_json`, `response_model=` gardé, sortie octet-identique).
- **P2 conventions** : **CR-C04** `commit_with_retry` non appliqué aux writers request-path (`tv_auth`/`accounts`/`categories`/services) ; **CR-C05** schéma dupliqué ORM↔migrations (warnings `duplicate column` à chaque boot fraîche, `database.py:92` vs `ADD COLUMN`).
- **P1/P2 tests** : **CR-T03** orchestration `sync_worker` (1390 LOC) non testée (helpers purs seulement) ; **CR-T04** lifespan/master-election/scheduler/auto-generate non testés (fixture skip le lifespan) ; **CR-T05** logique validation/enrichment non testée (`_check_one` mocké) ; **CR-T06** `api_key_service` (auth par-utilisateur) stubbé → jamais exécuté ; **CR-T07** 7 routers sans test HTTP (`accounts`/`categories`/`live`/`stream`/`sync`/`plex`/`api_keys`) ; **CR-T08** fixtures `:memory:` → WAL/busy_timeout no-op.
- **Debt** : **CR-C06/T09** aucun ruff/black/mypy/pytest-cov câblé ; **CR-T10** CI seulement sur `main` → `develop` non gardé + `PytestUnhandledThreadExceptionWarning` ignoré ; **CR-C07** `pydantic-settings` déclarée mais **non utilisée** ; **CR-C08** helpers `{edition-}` morts (`naming.py:7,24`) ; **CR-C09/T11** `HTTP_422_UNPROCESSABLE_ENTITY` déprécié en **prod** (`ai.py:330,365,416,423,463,1142`) ; **CR-C10** attr-bags anonymes (`accounts.py:50-56`, `main.py:143-150`) + M008 défini hors ordre de fichier ; **CR-A06/A07** reach-in symboles privés (`_serialize_vec`, `_movie_folder`) + `_build_versions` dupliqué API↔générateur ; **CR-S09** echo d'exceptions upstream + request-id client non borné ; **Docker 3.12 vs CI 3.13**.

## §11. Workflows
Voir `.claude/WORKFLOWS.md` (routeur intention→commande, auto-injecté au SessionStart). Commandes principales : `/feature`, `/refacto`, `/incident`, `/audit-cleanroom`, `/fix-cleanroom`, `/benchmark`→`/fix-bench-perf`, `/refresh-context`, `/sync-context`, `/release`, `/app-status`.
